/*
 * main.tsx — application shell and top-level pages.
 *
 * Renders the login screen, the navigation frame, and the primary pages: the
 * Overview command center (fleet-scoped executive dashboard fetched from /overview),
 * the SLA & Resilience page, the Network Telemetry page, and the device-detail view.
 * The Device Fleet and Access Points pages live in their own modules (InventoryFleet
 * and AccessPoints). Auth token handling and theme (dark/light/system) live here too.
 * The code is intentionally dense; the <Help> tooltips and docs/APPLICATION.md explain
 * what each section means for end users.
 */
import React,{useEffect,useRef,useState} from 'react'; import{createRoot}from'react-dom/client'; import{Activity,AlertTriangle,ArrowLeft,ArrowRight,Clock,Cpu,Eye,EyeOff,FileDown,Gauge,Globe,HelpCircle,LayoutDashboard,LockKeyhole,LogOut,MemoryStick,Moon,Network,Plus,Radio,RefreshCw,Route,Router,Server,Settings as SettingsIcon,ShieldCheck,Sun,Terminal,Trash2,Wifi}from'lucide-react'; import InventoryFleet from'./InventoryFleet'; import AccessPoints from'./AccessPoints'; import WanProviders from'./WanProviders'; import Help from'./Help'; import HelpCenter from'./HelpCenter'; import{usePager,Pager}from'./Paged'; import'./design-system.css'; import'./style.css'; import'./detail.css'; import'./inventory.css'; import'./auth.css'; import'./overview.css'; import'./pathres.css'; import{fmtPct,fmtPctDelta}from'./format';
const NAV_ICONS:Record<string,any>={'Overview':LayoutDashboard,'Device Fleet':Server,'Access Points':Wifi,'SLA & Resilience':Gauge,'Network Telemetry':Network,'WAN Providers':Globe,'Path Resilience':Route,'Help':HelpCircle,'Settings':SettingsIcon};
type Device={id:number;hostname:string;management_ip?:string;site:string;role:string;criticality:string;device_type:string;model?:string;active:boolean;match_status:string};
const getToken=()=>localStorage.getItem('token')||sessionStorage.getItem('token');
const clearToken=()=>{localStorage.removeItem('token');sessionStorage.removeItem('token')};
const systemTheme=matchMedia('(prefers-color-scheme: dark)');
systemTheme.addEventListener('change',event=>{if((localStorage.getItem('themePreference')||'system')==='system'){const theme=event.matches?'dark':'light';localStorage.setItem('theme',theme);document.documentElement.dataset.theme=theme}});
const api=async(path:string,options:RequestInit={})=>{const token=getToken();const r=await fetch('/api/v1'+path,{...options,headers:{...(options.body instanceof FormData?{}:{'Content-Type':'application/json'}),...(token?{Authorization:`Bearer ${token}`}:{})}});if(r.status===401&&path!=='/auth/login'){clearToken();location.reload();throw new Error('Session expired')}if(!r.ok){const payload=await r.json().catch(()=>({detail:'Request failed'}));const detail=Array.isArray(payload.detail)?payload.detail.map((x:any)=>x.msg||String(x)).join('; '):payload.detail;throw new Error(detail||'Request failed')}return r.status===204?null:r.json()};
function Login({done}:{done:()=>void}){const[account,setAccount]=useState<'user'|'admin'>('user');const[password,setPassword]=useState('');const[remember,setRemember]=useState(true);const[visible,setVisible]=useState(false);const[error,setError]=useState('');const[dark,setDark]=useState(()=>{const s=localStorage.getItem('theme');return s?s==='dark':matchMedia('(prefers-color-scheme: dark)').matches});useEffect(()=>{document.documentElement.dataset.theme=dark?'dark':'light'},[dark]);return <main className="login"><button className="theme-toggle" type="button" onClick={()=>{const nd=!dark;localStorage.setItem('theme',nd?'dark':'light');setDark(nd)}} aria-label={`Use ${dark?'light':'dark'} mode`}>{dark?<Sun/>:<Moon/>}</button><section className="login-wrap"><form onSubmit={async event=>{event.preventDefault();setError('');try{const data=await api('/auth/login',{method:'POST',body:JSON.stringify({username:account,password})});clearToken();(remember?localStorage:sessionStorage).setItem('token',data.access_token);done()}catch(x:any){setError(x.message)}}}><div className="login-brand"><img className="medline-logo" src="/medline-logo.jpg" alt="Medline logo"/><h1>Medline Canada</h1><p>Enterprise Network Reliability</p></div><div className="account-tabs" role="group" aria-label="Account type"><button type="button" className={account==='user'?'selected':''} onClick={()=>setAccount('user')}>Network User</button><button type="button" className={account==='admin'?'selected':''} onClick={()=>setAccount('admin')}>Administrator</button></div><label className="password-label" htmlFor="login-password">Password</label><div className="password-field"><LockKeyhole/><input id="login-password" type={visible?'text':'password'} placeholder="Enter your password" value={password} onChange={event=>setPassword(event.target.value)} autoComplete="current-password" required autoFocus/><button type="button" onClick={()=>setVisible(!visible)} aria-label={visible?'Hide password':'Show password'}>{visible?<EyeOff/>:<Eye/>}</button></div><label className="remember"><input type="checkbox" checked={remember} onChange={event=>setRemember(event.target.checked)}/><span>Remember me</span></label><p className="trust-note">Trusted sessions expire after 14 days or when you sign out.</p><button className="sign-in" type="submit">Sign in <ArrowRight/></button>{error&&<p className="login-error" role="alert">{error}</p>}<p className="restricted">Restricted access. Authorized personnel only.</p></form><footer><div>© 2026 Medline Canada Enterprise Network Reliability</div><div>Implemented by Payam · <a href="mailto:phessamfar@medline.com">phessamfar@medline.com</a></div><span className="system-status"><i/> System operational</span></footer></section></main>}
function PathResilience({administrator}:{administrator:boolean}){
  const[data,setData]=useState<any>(null);const[err,setErr]=useState('');const[tp,setTp]=useState<any>(null);
  const[ssh,setSsh]=useState<any>(null); // {site,device_id,plan,transcript,busy,msg}
  const load=()=>api('/path-resilience').then(setData).catch((x:any)=>setErr(x.message));
  useEffect(()=>{load()},[]);
  useEffect(()=>{api('/throughput/window?hours=168').then(setTp).catch(()=>{})},[]);
  const openSsh=async(b:any)=>{setSsh({site:b.site_code,device_id:b.dsw_device_id,transcript:'',pw:'',busy:false,msg:'',plan:null});try{const plan=await api(`/devices/${b.dsw_device_id}/collect-plan`);setSsh((s:any)=>s?{...s,plan}:s)}catch(x:any){setSsh((s:any)=>s?{...s,msg:x.message}:s)}};
  const submitConnect=async()=>{setSsh((s:any)=>({...s,busy:true,msg:'Approve the sign-in on your Microsoft Authenticator — this can take up to ~2 minutes.'}));try{const r=await api(`/devices/${ssh.device_id}/ssh-collect`,{method:'POST',body:JSON.stringify({password:ssh.pw})});if(r.status==='ok'){setSsh(null);await load()}else{setSsh((s:any)=>({...s,busy:false,msg:r.message||'SSH failed.'}))}}catch(x:any){setSsh((s:any)=>({...s,busy:false,msg:x.message||'SSH failed.'}))}};
  const submitSsh=async()=>{setSsh((s:any)=>({...s,busy:true,msg:''}));try{const r=await api(`/devices/${ssh.device_id}/manual-collect`,{method:'POST',body:JSON.stringify({transcript:ssh.transcript})});if(r.status==='ok'){setSsh(null);await load()}else{setSsh((s:any)=>({...s,busy:false,msg:r.message||'Could not parse the pasted output.'}))}}catch(x:any){setSsh((s:any)=>({...s,busy:false,msg:x.message}))}};
  if(err)return <div className="banner">{err}</div>;
  if(!data)return <div className="pr"><div className="pr-loading">Loading path resilience…</div></div>;
  const pct=fmtPct;
  // A switch stack is several physical switches that LogicMonitor monitors as ONE logical
  // device. Saying "28 devices (10 monitored)" reads as if 18 were unmonitored — they are
  // not. State chassis, units and stacks explicitly instead.
  const deviceLine=(b:any)=>{
    const p=b.physical_switches??b.device_count, u=b.monitored_units??b.monitored_count, st=b.stack_count??0;
    const sw=`${p} switch${p===1?'':'es'}`;
    if(u==null||p===u) return `${sw} · all monitored`;
    return `${sw} in ${u} monitored unit${u===1?'':'s'}${st?` (${st} stacked)`:''} · all monitored`;
  };
  const B=data.branches;
  const fully=B.filter((b:any)=>b.posture.indexOf('Fully')===0).length;
  const singleL3=B.filter((b:any)=>b.failover.has_routing_evidence&&b.failover.northbound_paths<=1).length;
  const chip=(cls:string,txt:string)=><span className={'pr-chip '+cls}>{txt}</span>;
  return <div className="pr">
    <h1 className="pr-h1">Provider &amp; Path Resilience</h1>
    <p className="pr-sub">Availability across switches and provider circuits, failover posture, and best routes per branch. As of {data.as_of}.</p>
    <div className="pr-sec">Fleet Summary</div>
    <div className="pr-kpis">
      <div className="pr-kpi info"><div className="k-l">BRANCHES <Help text="Distinct sites (branch offices) with at least one LogicMonitor-mapped device in Device Fleet."/></div><div className="k-v">{B.length}</div></div>
      <div className="pr-kpi ok"><div className="k-l">FULLY REDUNDANT <Help text="Branches that are east-west redundant (stacked distribution + dual-homed access) AND have either multiple L3 default paths or two or more WAN provider circuits."/></div><div className="k-v">{fully}</div></div>
      <div className="pr-kpi warn"><div className="k-l">SINGLE L3 DEFAULT <Help text="Branches whose DSW has at most one default route next-hop — a single Layer-3 path out, even if backup circuits or FHRP exist. HSRP/VRRP protects the first-hop gateway, not the northbound WAN path."/></div><div className="k-v">{singleL3}</div></div>
      <div className="pr-kpi"><div className="k-l">7-DAY PEAK <Help text="The busiest hourly fleet throughput (in+out) seen in the last 7 days. See Fleet Throughput below."/></div><div className="k-v">{tp&&tp.available?`${tp.peak.value} ${tp.peak.unit}`:'—'}</div></div>
    </div>
    {tp&&tp.available&&<><div className="pr-sec">Fleet Throughput · last 7 days (in + out)</div>
      <div className="pr-panel"><div className="pr-metrics">
        <div className="pr-metric"><div className="m-l">Average <Help text="Mean of the fleet's total in+out interface throughput across every hourly bucket in the last 7 days (each device's latest snapshot per hour is summed; empty hours carry the last value forward). Throughput = link speed x utilisation, both directions."/></div><div className="m-v">{tp.average.value} {tp.average.unit}</div></div>
        <div className="pr-metric"><div className="m-l">Busy-hour peak <Help text="The single highest hourly fleet throughput (in+out) in the last 7 days — the busiest moment. This is the honest 'how loaded does it get' figure, unlike the instantaneous Snapshot Utilisation card on Overview."/></div><div className="m-v">{tp.peak.value} {tp.peak.unit}</div></div>
        <div className="pr-metric"><div className="m-l">95th percentile <Help text="The throughput level that was exceeded in only 5% of the hourly buckets over the last 7 days. It represents a robust 'typical busy' level that ignores one-off spikes — the same 95th-percentile method carriers use for billing."/></div><div className="m-v">{tp.p95.value} {tp.p95.unit}</div></div>
      </div><div className="pr-ref" style={{marginTop:10}}>{tp.note}</div></div></>}
    <div className="pr-sec">Branch Resilience</div>
    {B.map((b:any)=>{const w=b.windows;const f=b.failover;
      const postureCls=b.posture.indexOf('Fully')===0?'ok':(b.posture.indexOf('Run')===0?'neutral':'warn');
      return <div className="pr-branch" key={b.site_code}>
        <div className="pr-branch-h">
          <div><span className="pr-site">{b.site_code}</span><span className="pr-city">{b.city} · {deviceLine(b)}<Help text={`${b.physical_switches} physical switch(es) at this branch, monitored as ${b.monitored_units} unit(s) because a stack of switches reports to LogicMonitor as ONE device — that is normal and correct, not a monitoring gap. ${b.stack_count} unit(s) are stacks, ${b.standalone_count} standalone. Every unit here is mapped and actively collected: nothing at this branch is unmonitored.`}/></span></div>
          <div style={{display:'flex',gap:8,alignItems:'center'}}>
            {administrator&&b.dsw_device_id&&<button className="pr-sshbtn" onClick={()=>openSsh(b)}><Terminal size={13}/> Pull via SSH</button>}
            {chip(postureCls,b.posture)}
          </div>
        </div>
        <div className="pr-branch-b">
          {b.wan_circuits&&b.wan_circuits.length>0&&<div className="pr-wan">{b.wan_circuits.map((c:any,i:number)=>
            <div className={'pr-circuit'+(c.is_primary?' primary':'')} key={i}><div className="c-p">{c.provider}{c.is_primary?' ①':''}</div><div className="c-m">{c.ip} · {c.status}</div></div>)}</div>}
          <div className="pr-tiles">
            {[['Rolling 7-day',w.rolling_7,'Trailing 7 days (last same-weekday to now) — always a full week of data, so it never reads "insufficient" early in the week.'],
              ['Week-to-date',w.wtd,'This calendar week so far (Monday 00:00 to now). Reads "insufficient" on Monday/Tuesday until enough of the week is observed.'],
              ['Previous week',w.prev_week,'The last complete calendar week (previous Monday to Sunday) — the figure used for weekly reporting.'],
              ['YTD',w.ytd,'January 1 to now, with each device’s pre-commissioning days trimmed. Coverage-gated at 90%.']].map(([l,win,ht]:any)=>
              <div className="pr-tile" key={l}><div className="t-l">{l} <Help text={ht+' Shown as best-path (branch kept a path if its best-covered device was up); pooled = strict per-device average.'}/></div><div className="t-v">{win.status==='ok'?pct(win.best_path):'Insufficient'}</div><div className="t-s">best-path{win.status==='ok'&&win.pooled!=null?` · pooled ${pct(win.pooled)}`:''}</div></div>)}
          </div>
          <div className="pr-chiprow">
            {chip(f.distribution_redundant?'ok':'warn',`Distribution ${f.distribution_redundant?'stacked':'single'}`)}
            {chip(f.access_redundant?'ok':'warn',`Access ${f.dual_homed_access} dual-homed`)}
            {chip(f.wan_redundant?'ok':(f.wan_circuits?'warn':'neutral'),`WAN ${f.wan_circuits} circuit(s)`)}
            {chip(f.has_routing_evidence?(f.northbound_paths>1?'ok':'warn'):'neutral',f.has_routing_evidence?`L3 default ${f.northbound_paths} path`:'L3: needs SSH')}
          </div>
          {f.has_routing_evidence?
            <div className="pr-tablewrap"><table className="pr-table"><thead><tr>{['Priority','Prefix','Next hop','Protocol','AD/Metric'].map(h=><th key={h}>{h}</th>)}</tr></thead>
              <tbody>{f.priority_routes.map((r:any,i:number)=><tr key={i}><td>{['①','②','③'][i]||r.priority}</td><td>{r.prefix}</td><td>{r.next_hop}</td><td>{r.protocol}</td><td>{r.ad_metric}</td></tr>)}</tbody></table></div>
            :<div className="pr-need">Priority routes &amp; HSRP populate once the branch DSW is collected over SSH{administrator?' — use "Pull via SSH" above':''} (LogicMonitor has no routing table).</div>}
          {f.findings&&f.findings.length>0&&<ul className="pr-findings">{f.findings.map((fn:any,i:number)=><li key={i} className={fn.severity==='warning'?'warn':'ok'}>{fn.text}</li>)}</ul>}
          {b.kb&&b.kb.length>0&&<div className="pr-kb"><div className="pr-kb-h">{administrator?'Recommended remediation · CCIE KB':'Recommended actions'}<Help text={administrator?'Each risk found at this branch, why it matters, the recommended fix, and copy-ready read-only commands to verify it. The app never runs these — an engineer reviews and applies them.':'Each risk found at this branch, why it matters in plain English, and what should change. The device configuration commands are shown to administrators only.'}/></div>{b.kb.map((k:any)=>
            <details className="pr-kb-item" key={k.id}><summary>🛠 {k.title}</summary><div className="pr-why">{k.why}</div><div className="pr-rec"><b>Recommended:</b> {k.recommendation}</div>{k.commands&&<pre className="pr-pre">{k.commands.join('\n')}</pre>}{k.reference&&<div className="pr-ref">Ref: {k.reference}</div>}</details>)}</div>}
        </div>
      </div>})}
    {ssh&&<div className="pr-modal" onClick={()=>!ssh.busy&&setSsh(null)}><div className="pr-modal-box" onClick={e=>e.stopPropagation()}>
      <div className="pr-modal-h"><b>Pull data from {ssh.site} DSW over SSH</b><button onClick={()=>!ssh.busy&&setSsh(null)}>✕</button></div>
      <p className="pr-why">Runs read-only Cisco <code>show</code> commands on <b>{ssh.plan?.management_ip||'the branch DSW'}</b> as <b>pa-phessamfar</b> — no configuration change is made. The password is used for one session and never stored.</p>
      <label className="dv-modal-label">Connect now — you'll approve the sign-in on Microsoft Authenticator</label>
      <div style={{display:'flex',gap:8}}>
        <input type="password" className="dv-modal-input" style={{flex:1}} value={ssh.pw} disabled={ssh.busy} autoFocus onChange={e=>setSsh((s:any)=>({...s,pw:e.target.value}))} onKeyDown={e=>e.key==='Enter'&&ssh.pw&&!ssh.busy&&submitConnect()} placeholder="SSH password"/>
        <button className="primary" style={{whiteSpace:'nowrap'}} onClick={submitConnect} disabled={ssh.busy||!ssh.pw||!ssh.plan?.management_ip}>{ssh.busy?'Waiting for approval…':'Connect & collect'}</button>
      </div>
      {ssh.msg&&<div className="pr-err" style={{display:'flex',alignItems:'center',gap:8}}>{ssh.busy&&<RefreshCw size={14} className="dv-spin"/>}{ssh.msg}</div>}
      <details style={{marginTop:16}}><summary style={{cursor:'pointer',fontSize:13}}>Or paste output manually (if the app can't reach the device)</summary>
        {ssh.plan?<pre className="pr-pre" style={{marginTop:12}}>{ssh.plan.script}</pre>:<div className="pr-loading">Loading commands…</div>}
        <textarea className="pr-ta" placeholder="Paste the SSH transcript here…" value={ssh.transcript} onChange={e=>setSsh((s:any)=>({...s,transcript:e.target.value}))}/>
        <div style={{display:'flex',justifyContent:'flex-end',marginTop:8}}><button className="primary" onClick={submitSsh} disabled={ssh.busy||!ssh.transcript.trim()}>{ssh.busy?'Parsing…':'Ingest transcript'}</button></div>
      </details>
      <div style={{display:'flex',justifyContent:'flex-end',marginTop:10}}><button onClick={()=>setSsh(null)} disabled={ssh.busy}>Close</button></div>
    </div></div>}
  </div>;
}

function App(){const[ready,setReady]=useState(!!getToken());const[role,setRole]=useState('');const[page,setPage]=useState('Overview');const[selected,setSelected]=useState<Device|null>(null);const[devices,setDevices]=useState<Device[]>([]);const[error,setError]=useState('');const[dark,setDark]=useState(()=>{const s=localStorage.getItem('theme');return s?s==='dark':matchMedia('(prefers-color-scheme: dark)').matches});const load=()=>api('/devices').then(setDevices).catch(x=>setError(x.message));useEffect(()=>{document.documentElement.dataset.theme=dark?'dark':'light'},[dark]);useEffect(()=>{if(ready){api('/auth/me').then(x=>setRole(x.role));load()}},[ready]);if(!ready)return <Login done={()=>setReady(true)}/>;const navigate=(x:string)=>{setSelected(null);setPage(x)};const menu=['Overview','Device Fleet','Access Points','SLA & Resilience','Network Telemetry','WAN Providers','Path Resilience','Help',...(role==='Administrator'?['Settings']:[])];return <div className="ds-shell"><nav className="ds-nav"><div className="ds-brand"><img src="/medline-logo.jpg" alt="Medline Canada"/><div className="n"><b>Medline</b><span>Canada</span></div></div><button className="ds-nav-item ds-signout" onClick={()=>{clearToken();setReady(false)}}><LogOut size={18}/><span>Sign out</span></button><div className="ds-nav-group">Monitoring</div>{menu.map(x=><button key={x} className={'ds-nav-item'+(page===x&&!selected?' active':'')} onClick={()=>navigate(x)}>{React.createElement(NAV_ICONS[x]||Activity,{size:18})}<span>{x}</span></button>)}<div className="ds-nav-spacer"/></nav><div className="ds-main"><div className="ds-topbar"><div className="t-title"><div className="t-eyebrow">Medline Canada · Enterprise Network</div><h1>{selected?selected.hostname:page}</h1></div><div className="t-actions"><button className="ds-icon-btn" onClick={()=>{const nd=!dark;localStorage.setItem('theme',nd?'dark':'light');setDark(nd)}} aria-label={`Use ${dark?'light':'dark'} mode`}>{dark?<Sun size={18}/>:<Moon size={18}/>}</button><span className="ds-pill"><ShieldCheck size={14}/> {role||'Authenticated'}</span></div></div><div className="ds-content">{error&&<div className="banner">{error}</div>}{selected?<DeviceDetail device={selected} back={()=>setSelected(null)} administrator={role==='Administrator'}/>:<>{page==='Overview'&&<Overview devices={devices} open={setSelected} navigate={navigate}/>} {page==='Device Fleet'&&<InventoryFleet token={getToken()||''} open={setSelected}/>} {page==='Access Points'&&<AccessPoints token={getToken()||''} administrator={role==='Administrator'}/>} {page==='SLA & Resilience'&&<SlaPage role={role}/>} {page==='Network Telemetry'&&<TelemetryPage/>} {page==='WAN Providers'&&<WanProviders token={getToken()||''} administrator={role==='Administrator'}/>} {page==='Help'&&<HelpCenter token={getToken()||''}/>} {page==='Path Resilience'&&<PathResilience administrator={role==='Administrator'}/>} {page==='Settings'&&role==='Administrator'&&<SettingsPage reload={load}/>}</>}</div></div></div>}
function Overview({devices,open,navigate}:{devices:Device[],open:(d:Device)=>void,navigate:(p:string)=>void}){
  const[o,setO]=useState<any>(null);const[err,setErr]=useState('');const[msg,setMsg]=useState('');
  const sitePg=usePager<any>(o?.sites||[]);
  useEffect(()=>{api('/overview').then(setO).catch(x=>setErr(x.message))},[]);
  const exportReport=async(kind:'excel'|'pptx')=>{setMsg('Generating '+(kind==='excel'?'Excel':'PowerPoint')+'…');try{const r=await api('/reports/executive/'+kind,{method:'POST'});const resp=await fetch('/api/v1/reports/'+r.filename,{headers:{Authorization:`Bearer ${getToken()}`}});const b=await resp.blob();const a=document.createElement('a');a.href=URL.createObjectURL(b);a.download=r.filename;a.click();URL.revokeObjectURL(a.href);setMsg('')}catch(x:any){setMsg(x.message)}};
  if(err)return <div className="ov"><div className="ov-summary"><p>{err}</p></div></div>;
  if(!o)return <div className="ov"><div className="ov-summary"><p>Loading executive overview…</p></div></div>;
  const openDev=(id:number|null)=>{if(id==null)return;const d=devices.find(x=>x.id===id);if(d)open(d);else navigate('Device Fleet')};
  const pct2=fmtPct,pct3=fmtPct; // single shared formatter (Number(v)+2dp, trailing zeros trimmed) — no longer two precisions
  const ago=(iso:string|null)=>{if(!iso)return '—';const s=(Date.now()-new Date(iso).getTime())/1000;if(s<90)return 'Just now';if(s<3600)return Math.round(s/60)+' min ago';if(s<86400)return Math.round(s/3600)+' h ago';return Math.round(s/86400)+' d ago'};
  const badge=(status:string)=>{const s=String(status).toUpperCase();return /CRIT|FAULT|DOWN/.test(s)?'crit':/HIGH|WARN|DEGRAD/.test(s)?'warn':/UNKNOWN|INSUFF/.test(s)?'unknown':'ok'};
  const stbg=(st:string)=>st==='unknown'?'var(--ov-surface-high)':`var(--ov-${st}-bg)`;const stfg=(st:string)=>st==='unknown'?'var(--ov-faint)':`var(--ov-${st})`;
  const h=o.header,g=o.global_sla,cr=o.criticality,av=o.availability_7d;
  const slaClass=g.status==='Target met'?'ok':g.status==='Insufficient evidence'?'unknown':g.status==='Below target'?'crit':'warn';
  const slaColor=stfg(slaClass);
  const C=2*Math.PI*52,frac=g.current==null?0:Math.max(0,Math.min(1,g.current/100));
  const D=2*Math.PI*44;let acc=0;const segs:[string,number][]=[['var(--ov-crit)',cr.bands.Critical||0],['var(--ov-warn)',cr.bands.High||0],['var(--ov-info)',cr.bands.Medium||0],['var(--ov-faint)',cr.bands.Low||0]];
  const barH=(v:any)=>v==null?8:Math.max(0,Math.min(100,v));
  return <div className="ov">
    <div className="ov-exec">
      <div><div className="ov-eyebrow">{h.org}</div><h1 className="ov-title">{h.title}</h1></div>
      <div className="ov-status">
        <div className="k"><span>API Status</span><b><span className={'ov-chip '+(h.api_status==='Connected'?'ok':'warn')}><span className="ov-dot"/>{h.api_status}</span></b></div>
        <div className="k"><span>Database</span><b><span className="ov-chip ok"><span className="ov-dot"/>{h.database_status}</span></b></div>
        <div className="k"><span>Last Sync</span><b className={h.data_stale?'ov-stale':''}>{ago(h.last_sync)}{h.data_stale?' · STALE':''}</b></div>
        <div className="k"><span>Fleet Coverage</span><b>{h.fleet_coverage.monitored}/{h.fleet_coverage.total}</b></div>
        <div className="k"><span>LM Coverage</span><b>{h.lm_coverage.matched}/{h.lm_coverage.total}</b></div>
        <div className="k"><span>SLA Evidence</span><b>{fmtPct(h.sla_evidence_coverage)}</b></div>
        <div className="k"><span>Data Quality</span><b><span className={'ov-chip '+(h.data_quality==='High'?'ok':h.data_quality==='Medium'?'warn':'crit')}>{h.data_quality}</span></b></div>
      </div>
    </div>
    <div style={{display:'flex',gap:10,justifyContent:'flex-end',alignItems:'center',flexWrap:'wrap',marginBottom:20}}>
      {msg&&<span className="ov-sub">{msg}</span>}
      <button className="ov-export" onClick={()=>exportReport('excel')}><FileDown size={15}/> Executive Excel</button>
      <button className="ov-export" onClick={()=>exportReport('pptx')}><FileDown size={15}/> Executive PowerPoint</button>
    </div>
    <div className="ov-kpis">
      <div className="ov-card"><h3>Global SLA Status<Help text="Fleet network availability over the trailing 30 days vs the SLA target, with the change (delta) and status. The ring fills to the availability %. Coverage-gated — shows Insufficient below 90% evidence."/></h3><div className="center">
        <div style={{position:'relative',width:150,height:150}}>
          <svg viewBox="0 0 120 120" width="150" height="150" role="img" aria-label={`Global SLA ${g.current!=null?fmtPct(g.current):'insufficient evidence'} against a ${g.target}% target`}><circle cx="60" cy="60" r="52" fill="none" stroke="var(--ov-track)" strokeWidth="8"/><circle cx="60" cy="60" r="52" fill="none" stroke={slaColor} strokeWidth="8" strokeLinecap="round" strokeDasharray={C} strokeDashoffset={C*(1-frac)} transform="rotate(-90 60 60)"/></svg>
          <div style={{position:'absolute',inset:0,display:'grid',placeItems:'center'}}><div className="ov-ring-val" style={{color:slaColor}}>{pct2(g.current)}</div></div>
        </div>
        <div className="ov-sub">Target {g.target}% · {fmtPctDelta(g.delta)}</div>
        <span className={'ov-chip '+slaClass}>{g.status}</span>
      </div></div>
      <div className="ov-card"><h3>Device Criticality<Help text="How important each device is to the BUSINESS — Critical / High / Medium / Low — set by hand in Settings → Device Inventory. This is NOT the device's current health: a Medium-criticality switch can still be faulty right now, which is what Core Infrastructure Health below shows. The centre number is the total fleet size and the ring shows the mix. If every device sits at Medium, that is the default — it means criticality has not been classified yet, not that every device was assessed as medium importance."/></h3><div className="center">
        <div style={{position:'relative',width:140,height:140}}>
          <svg viewBox="0 0 120 120" width="140" height="140" role="img" aria-label={`Device criticality mix: ${cr.total} total devices, ${cr.bands.Critical} critical, ${cr.bands.High} high, ${cr.bands.Medium} medium, ${cr.bands.Low} low`}><circle cx="60" cy="60" r="44" fill="none" stroke="var(--ov-track)" strokeWidth="12"/>{segs.map(([c,v],i)=>{const len=cr.total?D*v/cr.total:0;const el=<circle key={i} cx="60" cy="60" r="44" fill="none" stroke={c} strokeWidth="12" strokeDasharray={`${len} ${D-len}`} strokeDashoffset={-acc} transform="rotate(-90 60 60)"/>;acc+=len;return el})}</svg>
          <div style={{position:'absolute',inset:0,display:'grid',placeItems:'center',textAlign:'center'}}><div><div style={{fontSize:26,fontWeight:700}}>{cr.total}</div><div className="ov-sub">TOTAL</div></div></div>
        </div>
        <div className="ov-legend"><span><i style={{background:'var(--ov-crit)'}}/>Crit {cr.bands.Critical||0}</span><span><i style={{background:'var(--ov-warn)'}}/>High {cr.bands.High||0}</span><span><i style={{background:'var(--ov-info)'}}/>Med {cr.bands.Medium||0}</span><span><i style={{background:'var(--ov-faint)'}}/>Low {cr.bands.Low||0}</span></div>
      </div></div>
      <div className="ov-card"><h3>7-Day Availability<Help text="Fleet availability for each of the last 7 days on a 0–100% scale. Red bars mark days below the SLA target; exact values, coverage, and cause are available on hover."/></h3><div className="ov-bars" role="img" aria-label={`7-day availability, 0 to 100 percent scale, target ${av.target}%: ${av.series.map((d:any)=>`${d.day} ${d.availability!=null?fmtPct(d.availability):d.status}`).join(', ')}`}>{av.series.map((d:any,i:number)=><div key={i} className={'ov-bar'+(d.abnormal?' abnormal':'')+(d.availability==null?' nodata':'')} style={{height:barH(d.availability)+'%'}} title={`${d.day}: ${d.availability!=null?fmtPct(d.availability):d.status}${d.cause?' — '+d.cause:''}`}/>)}</div><div className="ov-bars-axis"><span>0% · -6d</span><span>Target {av.target}% · Today</span></div></div>
      <div className="ov-card"><h3>Snapshot Utilisation<Help text="Point-in-time interface load (in + out) across all monitored up interfaces at the last poll, estimated as speed × utilisation. It is an instantaneous snapshot on the monitored fleet only — NOT a busy-hour, average, or WAN-egress figure. This number WILL differ between two environments (e.g. dev vs production) or between two page loads, because it reflects whatever each environment's own collector last polled from the live network — real traffic moves minute to minute. For a stable, comparable figure use Fleet Throughput on Path Resilience (7-day average/peak) instead. Devices are polled one at a time, not all at once, so this is really a blend of snapshots taken over the last refresh cycle — the stamp below shows the OLDEST of those (the most conservative bound), not the fleet's freshest sync shown in the page header. A peak/average WAN-egress throughput metric is planned."/></h3>{o.throughput.available?<div className="center"><div className="ov-ring-val">{o.throughput.value} {o.throughput.unit}</div><small style={{color:'var(--ov-dim)',fontFamily:'var(--ov-mono)',fontSize:11,marginTop:6}}>instant · in+out · monitored fleet</small><small style={{color:'var(--ov-faint)',fontFamily:'var(--ov-mono)',fontSize:10,marginTop:2}}>oldest contributing poll: {ago(o.throughput.oldest_contributing_sync)}</small></div>:<div className="ov-na"><AlertTriangle size={22} color="var(--ov-warn)"/><b>NOT MONITORED</b><small>{o.throughput.reason}</small></div>}</div>
    </div>
    <div className="ov-summary"><h2>Executive Summary<Help text="A plain-language, auto-generated narrative of fleet reliability: current availability vs target, devices lacking evidence, the lowest-performing site, and telemetry coverage. Deterministic — built from the live numbers, not written by hand."/></h2><p>{o.summary}</p></div>
    <div className="ov-panel"><div className="ov-panel-head"><h2><ShieldCheck size={15}/> Core Infrastructure Health<Help text="The devices that most need attention RIGHT NOW, ranked by current problem severity × business criticality (unreachable, hardware fault, high CPU/memory/temperature, warnings). The CRITICAL / WARNING labels here describe a device's live condition — they are a different thing from the Device Criticality ring above, which describes how important a device is to the business. A device can show CRITICAL here while being Medium criticality: it is genuinely faulty, but it is not one of the most business-critical devices. Click a row to open its full device detail."/></h2><span className="tag">Top {o.core_infrastructure.length} by risk × criticality</span></div>
      {o.core_infrastructure.map((c:any)=>{const st=badge(c.status);return <div key={c.device_id} className="ov-core-row" onClick={()=>openDev(c.device_id)}><div className="ov-core-l"><div className="ov-core-ic" style={{background:stbg(st)}}><Router size={18} color={stfg(st)}/></div><div><div className="ov-core-name">{c.hostname}</div><div className="ov-core-meta">{c.city} · {c.model}</div></div></div><div className="ov-core-r"><div className="ov-metric"><span>{c.metric}</span><b>{c.value}</b></div><span className={'ov-badge '+st}>{c.status}</span></div></div>})}
    </div>
    <div className="ov-panel-head" style={{background:'transparent',border:0,padding:'0 0 12px'}}><h2 style={{fontSize:20,textTransform:'none',fontFamily:'var(--ov-sans)',fontWeight:700}}>What leadership can act on now<Help text="The top few prioritised, business-impact-ordered actions (P1/P2) derived from unreachable/critical devices, single-point-of-failure exposure, and SLA risk. Click a card to open the affected device."/></h2><span className="tag">Priority ordered</span></div>
    <div className="ov-actions">{o.actions.length?o.actions.map((a:any)=><div key={a.priority} className={'ov-action '+(a.severity==='P1'?'p1':a.severity==='P2'?'p2':'p3')} onClick={()=>openDev(a.device_id)}><div className="ov-action-top"><span className="sev">{a.severity}</span><ArrowRight size={16} color="var(--ov-faint)"/></div><h4>{a.title}</h4><p>{a.detail}</p></div>):<div className="ov-actions-empty">No leadership actions — the network is operating within targets.</div>}</div>
    {o.business_units?.length?<div className="ov-panel"><div className="ov-panel-head"><h2><Activity size={15}/> Business Unit Reliability<Help text="The fleet rolled up by business unit (e.g. Healthcare vs Dental): YTD and 30-day availability across that unit's devices, physical device count, sites, and open incidents. YTD is measured from each site's commissioning date, so a mid-year project shows a real number with a 'since' note rather than 'Insufficient'."/></h2><span className="tag">{o.business_units.length} units</span></div>
      <div className="ov-bu">{o.business_units.map((b:any,i:number)=><div key={i} className="ov-bu-card"><div className="ov-bu-top"><h3>{b.business_unit}</h3><span className={'ov-badge '+badge(b.status)}>{b.status}</span></div><div className="ov-bu-avail">{b.availability_ytd!=null?<b>{pct3(b.availability_ytd)}</b>:<b className="muted">Insufficient</b>}<small>YTD availability{b.since?` · since ${b.since}`:''}</small></div><div className="ov-bu-stats"><div><span>30-Day</span><b>{b.availability_30d!=null?pct3(b.availability_30d):'—'}</b></div><div><span>Devices</span><b>{b.devices}</b></div><div><span>Sites</span><b>{b.sites}</b></div><div><span>Incidents</span><b>{b.incidents}</b></div></div>{b.reason?<div className="ov-bu-note">{b.reason}</div>:null}</div>)}</div>
    </div>:null}
    <div className="ov-panel"><div className="ov-panel-head"><h2><Activity size={15}/> Site Reliability<Help text="Each site's rollup: YTD and 30-day availability (from that site's devices), device health, incident count, criticality, and status. Coverage-gated per site; 'Insufficient' where evidence is thin. The Devices column counts PHYSICAL switches — a stack of 6 switches counts as 6 here, even though LogicMonitor monitors that stack as one device. Everything shown is monitored."/></h2><span className="tag">{o.sites.length} sites</span></div>
      <div className="ov-table-wrap"><table className="ov-table"><thead><tr>{['Site','City','Province','Business Unit','Availability YTD','30-Day','Devices','Device Health','Incidents','Status'].map(x=><th key={x}>{x}</th>)}</tr></thead><tbody>{sitePg.slice.map((s:any,i:number)=><tr key={i}><td className="mono"><b>{s.site_code||'—'}</b></td><td>{s.city}</td><td>{s.province||'—'}</td><td>{s.business_unit}</td><td className="mono" title={s.reason||''}>{s.availability_ytd!=null?pct3(s.availability_ytd):<span className="muted">Insufficient</span>}{s.since&&s.availability_ytd!=null?<small style={{display:'block',color:'var(--ov-faint)',fontSize:10,fontFamily:'var(--ov-sans)'}}>since {s.since}</small>:null}</td><td className="mono">{s.availability_30d!=null?pct3(s.availability_30d):<span className="muted">—</span>}</td><td className="mono">{s.devices}</td><td><span className={'ov-badge '+badge(s.device_health)}>{s.device_health}</span></td><td className="mono">{s.critical_devices}</td><td><span className={'ov-badge '+badge(s.status)}>{s.status}</span></td></tr>)}</tbody></table></div>{o.sites.length>0&&<Pager {...sitePg} label="sites"/>}
    </div>
  </div>;
}
function Fleet({devices,reload,open}:{devices:Device[],reload:()=>void,open:(d:Device)=>void}){return <section className="panel"><div className="panelhead"><h2>Managed inventory</h2><button onClick={async()=>{await api('/reports/generate',{method:'POST'});alert('Report generated')}}><FileDown size={16}/> Generate report</button></div><table><thead><tr><th>Device</th><th>Site</th><th>Role</th><th>Model</th><th>Type</th><th>Mapping</th><th></th></tr></thead><tbody>{devices.map(d=><tr className="clickrow" onClick={()=>open(d)}><td><b className="device-link">{d.hostname}</b><small>{d.management_ip||'No IP'}</small></td><td>{d.site}</td><td>{d.role}</td><td>{d.model||'Not available'}</td><td><span className="pill">{d.device_type.replace('_',' ')}</span></td><td>{d.match_status}</td><td><button className="icon" aria-label="Delete" onClick={async e=>{e.stopPropagation();if(confirm(`Remove ${d.hostname} from local inventory?`)){await api('/devices/'+d.id,{method:'DELETE'});reload()}}}><Trash2 size={16}/></button></td></tr>)}</tbody></table>{!devices.length&&<div className="empty">No devices configured.</div>}</section>}
function nodeGlyph(kind:string,cx:number,cy:number,color:string,scale=1){
  const s=(v:number)=>v*scale;
  if(kind==='ap')return <g transform={`translate(${cx} ${cy})`} stroke={color} strokeWidth={1.6} fill="none" strokeLinecap="round"><path d={`M ${-s(9)} ${-s(1)} A ${s(9)} ${s(9)} 0 0 1 ${s(9)} ${-s(1)}`}/><path d={`M ${-s(5.5)} ${s(2.5)} A ${s(5.5)} ${s(5.5)} 0 0 1 ${s(5.5)} ${s(2.5)}`}/><circle cx={0} cy={s(6)} r={s(1.7)} fill={color} stroke="none"/></g>;
  if(kind==='sw')return <g transform={`translate(${cx} ${cy})`} stroke={color} strokeWidth={1.6} fill="none" strokeLinecap="round" strokeLinejoin="round"><rect x={-s(11)} y={-s(7)} width={s(22)} height={s(14)} rx={s(2)}/><line x1={-s(11)} y1={0} x2={s(11)} y2={0}/><circle cx={-s(6)} cy={s(3.6)} r={s(0.9)} fill={color} stroke="none"/><circle cx={-s(2)} cy={s(3.6)} r={s(0.9)} fill={color} stroke="none"/><circle cx={s(2)} cy={s(3.6)} r={s(0.9)} fill={color} stroke="none"/></g>;
  return <g transform={`translate(${cx} ${cy})`} stroke={color} strokeWidth={1.6} fill="none" strokeLinecap="round"><rect x={-s(8)} y={-s(8)} width={s(16)} height={s(16)} rx={s(2)}/><line x1={-s(8)} y1={-s(2)} x2={s(8)} y2={-s(2)}/></g>;
}
const TOPO_DARK={canvas:'#0b0f10',grid:'rgba(255,255,255,.045)',node:'#181c1e',chip:'#0b0f10',chipStroke:'#313537',nameFill:'#1c2022',nameStroke:'#424751',text:'#e0e3e5',dim:'#8fb0c8',lldp:'#75d2fb',cdp:'#a8caef',dual:'#ffb4ab',centerFill:'#1c2022',centerStroke:'#a6c8ff',labelBg:'#00539b',labelStroke:'#a6c8ff',labelText:'#d4e3ff'};
const TOPO_LIGHT={canvas:'#eef2f6',grid:'rgba(0,45,90,.07)',node:'#ffffff',chip:'#ffffff',chipStroke:'#c2c7ce',nameFill:'#ffffff',nameStroke:'#c2c7ce',text:'#191c1e',dim:'#3f5b6d',lldp:'#00658f',cdp:'#345575',dual:'#ba1a1a',centerFill:'#d4e3ff',centerStroke:'#00539b',labelBg:'#00539b',labelStroke:'#00539b',labelText:'#ffffff'};
function useTheme(){
  const get=()=>typeof document!=='undefined'&&document.documentElement.dataset.theme==='light'?'light':'dark';
  const[t,setT]=useState(get);
  useEffect(()=>{const o=new MutationObserver(()=>setT(get()));o.observe(document.documentElement,{attributes:true,attributeFilter:['data-theme']});return()=>o.disconnect()},[]);
  return t;
}
function abbrevPort(p:string|null){
  if(!p)return p;
  const map:[string,string][]=[['TwentyFiveGigE','Twe'],['TenGigabitEthernet','Te'],['FortyGigabitEthernet','Fo'],['HundredGigE','Hu'],['GigabitEthernet','Gi'],['FastEthernet','Fa'],['Ethernet','Eth'],['Port-channel','Po'],['Management','Mgmt'],['Loopback','Lo']];
  for(const[lng,sh]of map){if(p.toLowerCase().startsWith(lng.toLowerCase()))return sh+p.slice(lng.length)}
  return p;
}
function NeighborTopology({device,rows}:{device:Device,rows:any[]}){
  const theme=useTheme();const P=theme==='light'?TOPO_LIGHT:TOPO_DARK;
  const parse=(r:any)=>{const li=String(r['Local Interface']||'').trim();const m=li.match(/ on (\S+)(?: via (\S+))?/i);const remote=String(r['Remote Port']||'').trim();const realRemote=remote&&!/not available|not monitored|unknown/i.test(remote);return{name:String(r['Neighbor']||li||'Unknown'),protocol:String(r['Protocol']||'').toUpperCase(),local:m?m[1]:(li||null),remote:realRemote?remote:(m&&m[2]?m[2]:null)}};
  const nb=rows.map(parse);
  const groups=new Map<string,{name:string,links:any[]}>();
  nb.forEach(n=>{const k=n.name.toLowerCase();const g=groups.get(k)||{name:n.name,links:[]};g.links.push(n);groups.set(k,g)});
  const nodes=[...groups.values()].map(g=>{const seen=new Set<string>();const ulinks:any[]=[];[...g.links].sort((a,b)=>(a.protocol==='CDP'?0:1)-(b.protocol==='CDP'?0:1)).forEach(l=>{const key=abbrevPort(l.local)||'?';if(seen.has(key))return;seen.add(key);ulinks.push(l)});return{name:g.name,ulinks}});
  const anyDual=nodes.some(g=>g.ulinks.length>1);
  const kind=(name:string)=>/wap|(?:^|[-_])ap(?:[-_]|$)/i.test(name)?'ap':/dsw|asw|sw|switch|rtr|router/i.test(name)?'sw':'dev';
  const nodeColor=(g:{ulinks:any[]})=>g.ulinks.length>1?P.dual:(g.ulinks[0].protocol==='CDP'?P.cdp:P.lldp);
  const linkColor=(g:{ulinks:any[]},lk:any)=>g.ulinks.length>1?P.dual:(lk.protocol==='CDP'?P.cdp:P.lldp);
  const N=nodes.length;
  const R=Math.max(170,Math.min(150+N*13,380));
  const nodeR=22,centerR=32;
  const labelW=(s:string)=>s.length*6.6+14;
  const maxLW=Math.max(80,labelW(device.hostname),...nodes.map(g=>labelW(g.name)));
  const pad=Math.min(300,Math.round(70+maxLW));
  const vb=2*(R+pad);
  const pos=(i:number)=>{const a=(-90+i*360/Math.max(1,N))*Math.PI/180;return{a,x:R*Math.cos(a),y:R*Math.sin(a)}};
  const F='\'JetBrains Mono\',ui-monospace,monospace';
  const svgRef=useRef<SVGSVGElement>(null);
  const[box,setBox]=useState({x:-(R+pad),y:-(R+pad),w:vb,h:vb});
  const boxRef=useRef(box);boxRef.current=box;
  const drag=useRef<{sx:number,sy:number,ox:number,oy:number}|null>(null);
  useEffect(()=>{setBox({x:-(R+pad),y:-(R+pad),w:vb,h:vb})},[device.hostname,rows.length,vb]);
  const toUser=(cx:number,cy:number)=>{const svg=svgRef.current!;const r=svg.getBoundingClientRect();const b=boxRef.current;const s=Math.min(r.width/b.w,r.height/b.h)||1;const ox=(r.width-b.w*s)/2,oy=(r.height-b.h*s)/2;return{ux:b.x+(cx-r.left-ox)/s,uy:b.y+(cy-r.top-oy)/s}};
  useEffect(()=>{const svg=svgRef.current;if(!svg)return;const onWheel=(e:WheelEvent)=>{e.preventDefault();const b=boxRef.current;const{ux,uy}=toUser(e.clientX,e.clientY);const f=e.deltaY<0?0.85:1/0.85;const w=Math.min(Math.max(b.w*f,vb/10),vb*3);setBox({x:ux-(ux-b.x)*(w/b.w),y:uy-(uy-b.y)*(w/b.w),w,h:w})};svg.addEventListener('wheel',onWheel,{passive:false});return()=>svg.removeEventListener('wheel',onWheel)},[vb]);
  const zoomAt=(f:number)=>setBox(b=>{const cx=b.x+b.w/2,cy=b.y+b.h/2;const w=Math.min(Math.max(b.w*f,vb/10),vb*3);return{x:cx-(cx-b.x)*(w/b.w),y:cy-(cy-b.y)*(w/b.w),w,h:w}});
  const reset=()=>setBox({x:-(R+pad),y:-(R+pad),w:vb,h:vb});
  const onDown=(e:React.MouseEvent)=>{drag.current={sx:e.clientX,sy:e.clientY,ox:boxRef.current.x,oy:boxRef.current.y}};
  const onMove=(e:React.MouseEvent)=>{if(!drag.current)return;const svg=svgRef.current!;const r=svg.getBoundingClientRect();const b=boxRef.current;const s=Math.min(r.width/b.w,r.height/b.h)||1;setBox({x:drag.current.ox-(e.clientX-drag.current.sx)/s,y:drag.current.oy-(e.clientY-drag.current.sy)/s,w:b.w,h:b.h})};
  const onUp=()=>{drag.current=null};
  const total=nodes.reduce((a,g)=>a+g.ulinks.length,0);
  return <div className="dv-topo" style={{background:P.canvas,backgroundImage:`linear-gradient(${P.grid} 1px,transparent 1px),linear-gradient(90deg,${P.grid} 1px,transparent 1px)`,backgroundSize:'24px 24px'}}>
    <div className="dv-topo-legend">
      <div className="dv-topo-title">{device.hostname}</div>
      <div className="dv-topo-sub">{total} link{total===1?'':'s'} · {N} device{N===1?'':'s'}</div>
      <div className="dv-legend-row"><span className="dv-legend-dot" style={{background:P.lldp}}/> LLDP</div>
      <div className="dv-legend-row"><span className="dv-legend-dot" style={{background:P.cdp}}/> CDP</div>
      {anyDual&&<div className="dv-legend-row"><span className="dv-legend-dot" style={{background:P.dual}}/> Redundant link</div>}
    </div>
    <div className="dv-topo-zoom">
      <button onClick={()=>zoomAt(0.8)} aria-label="Zoom in" title="Zoom in">+</button>
      <button onClick={()=>zoomAt(1.25)} aria-label="Zoom out" title="Zoom out">−</button>
      <button onClick={reset} aria-label="Reset view" title="Reset view">⤢</button>
    </div>
    <svg ref={svgRef} className="dv-topo-svg" viewBox={`${box.x} ${box.y} ${box.w} ${box.h}`} onMouseDown={onDown} onMouseMove={onMove} onMouseUp={onUp} onMouseLeave={onUp} style={{cursor:'grab'}} role="img" aria-label={`CDP and LLDP neighbor topology for ${device.hostname}`}>
      {nodes.map((g,i)=>{const{x,y}=pos(i);const dist=Math.hypot(x,y)||1;const px=-y/dist,py=x/dist;const L=g.ulinks.length;return <g key={'e'+i}>{g.ulinks.map((lk,j)=>{const off=L>1?(j-(L-1)/2)*20:0;const cxp=x/2+px*off*2.2,cyp=y/2+py*off*2.2;return <path key={j} d={`M0 0 Q ${cxp.toFixed(1)} ${cyp.toFixed(1)} ${x.toFixed(1)} ${y.toFixed(1)}`} fill="none" stroke={linkColor(g,lk)} strokeWidth={L>1?2.6:2.2} strokeOpacity={0.7}/>})}</g>})}
      {nodes.map((g,i)=>{const{x,y}=pos(i);const dist=Math.hypot(x,y)||1;const px=-y/dist,py=x/dist;const L=g.ulinks.length;const dual=L>1;return <g key={'p'+i}>{g.ulinks.map((lk,j)=>{const off=L>1?(j-(L-1)/2)*20:0;const cxp=x/2+px*off*2.2,cyp=y/2+py*off*2.2;const bez=(t:number):[number,number]=>{const mt=1-t;return[2*mt*t*cxp+t*t*x,2*mt*t*cyp+t*t*y]};const lp=abbrevPort(lk.local),rp=abbrevPort(lk.remote);const chip=(p:[number,number],txt:string,col:string,k:string)=>{const w=txt.length*6.2+10;return <g key={k}><rect x={p[0]-w/2} y={p[1]-8} width={w} height={16} rx={3} fill={P.chip} stroke={dual?P.dual:P.chipStroke}/><text x={p[0]} y={p[1]+3} textAnchor="middle" fontFamily={F} fontSize={9} fill={col}>{txt}</text></g>};return <g key={j}>{lp?chip(bez(0.4),lp,P.text,'l'):null}{rp?chip(bez(0.75),rp,P.dim,'r'):null}</g>})}</g>})}
      {nodes.map((g,i)=>{const{a,x,y}=pos(i);const c=nodeColor(g);const cosA=Math.cos(a);const anchor:'start'|'end'|'middle'=cosA>0.25?'start':cosA<-0.25?'end':'middle';const lr=R+nodeR+10;const lx=lr*cosA,ly=lr*Math.sin(a);const w=labelW(g.name);const rx=anchor==='start'?lx-6:anchor==='end'?lx-w+6:lx-w/2;const dual=g.ulinks.length>1;return <g key={'n'+i}><circle cx={x} cy={y} r={nodeR} fill={P.node} stroke={c} strokeWidth={dual?2.7:2}/>{nodeGlyph(kind(g.name),x,y,c)}<title>{`${g.name} · ${g.ulinks.length} link${g.ulinks.length===1?'':'s'}`+g.ulinks.map(lk=>`\n  ${abbrevPort(lk.local)||'?'} → ${abbrevPort(lk.remote)||'?'} (${lk.protocol})`).join('')}</title><rect x={rx} y={ly-9} width={w} height={18} rx={4} fill={P.nameFill} stroke={dual?P.dual:P.nameStroke}/><text x={lx} y={ly+4} textAnchor={anchor} fontFamily={F} fontSize={10} fill={P.text}>{g.name}</text></g>})}
      <circle cx={0} cy={0} r={centerR} fill={P.centerFill} stroke={P.centerStroke} strokeWidth={2.5}/>
      {nodeGlyph('sw',0,0,P.centerStroke,1.25)}
      <rect x={-labelW(device.hostname)/2} y={centerR+6} width={labelW(device.hostname)} height={20} rx={4} fill={P.labelBg} stroke={P.labelStroke}/>
      <text x={0} y={centerR+20} textAnchor="middle" fontFamily={F} fontSize={11} fontWeight={600} fill={P.labelText}>{device.hostname}</text>
    </svg>
  </div>;
}
function DeviceDetail({device,back,administrator}:{device:Device,back:()=>void,administrator:boolean}){
  const[data,setData]=useState<any>(null);
  const[tab,setTab]=useState('Health Data');
  const[sshOpen,setSshOpen]=useState(false);const[transcript,setTranscript]=useState('');const[sshBusy,setSshBusy]=useState(false);const[sshMsg,setSshMsg]=useState('');const[cfg,setCfg]=useState<string|null>(null);const[plan,setPlan]=useState<any>(null);const[pw,setPw]=useState('');
  const load=()=>api(`/devices/${device.id}/detail`).then(setData);
  useEffect(()=>{load()},[device.id]);
  const openCollect=async()=>{setSshMsg('');setTranscript('');setPw('');setSshOpen(true);try{setPlan(await api(`/devices/${device.id}/collect-plan`))}catch{}};
  const runConnect=async()=>{setSshBusy(true);setSshMsg('Approve the sign-in on your Microsoft Authenticator — this can take up to ~2 minutes.');try{const r=await api(`/devices/${device.id}/ssh-collect`,{method:'POST',body:JSON.stringify({password:pw})});if(r.status==='ok'){setPw('');setSshOpen(false);await load()}else{setSshMsg(r.message||'SSH failed.')}}catch(x:any){setSshMsg(x.message||'SSH failed.')}finally{setSshBusy(false)}};
  const runCollect=async()=>{setSshBusy(true);setSshMsg('');try{const r=await api(`/devices/${device.id}/manual-collect`,{method:'POST',body:JSON.stringify({transcript})});if(r.status==='ok'){setTranscript('');setSshOpen(false);await load()}else{setSshMsg(r.message||'Could not parse the pasted output.')}}catch(x:any){setSshMsg(x.message)}finally{setSshBusy(false)}};
  const viewCfg=async()=>{try{const r=await api(`/devices/${device.id}/ssh-config`);setCfg(r.config)}catch(x:any){setCfg('Unable to load running-config.')}};
  if(!data)return <div className="detail-view"><div className="dv-panel dv-loading">Loading device details…</div></div>;
  const table=data.tables.find((x:any)=>x.name===tab)||data.tables[0];
  const HIDDEN_COLUMNS:{[k:string]:string[]}={'CDP-LLDP Neighbors':['Management IP','Platform/Model','Remote Port']};
  const displayColumns=table.columns.filter((c:string)=>!(HIDDEN_COLUMNS[table.name]||[]).includes(c));
  const rowsOf=(name:string)=>(data.tables.find((x:any)=>x.name===name)?.rows||[]);
  const health=rowsOf('Health Data')[0]||{};
  const ping=rowsOf('Ping Quality')[0]||{};
  const num=(v:any)=>typeof v==='number'&&isFinite(v)?v:(typeof v==='string'&&v.trim()!==''&&!isNaN(Number(v))?Number(v):null);
  const missingText=(v:any)=>typeof v==='string'&&/not available|not monitored|not collected|baseline pending|insufficient/i.test(v);
  const fmtUptime=(v:any)=>{const n=num(v);if(n==null)return'N/A';const d=Math.floor(n/86400),h=Math.floor((n%86400)/3600);return d>0?`${d}d ${h}h`:`${h}h ${Math.floor((n%3600)/60)}m`};
  const clean=(v:any)=>v&&!missingText(v)?String(v):'N/A';
  const availabilityPrecise=num(ping['24h Availability']);
  const availability=availabilityPrecise==null?null:Math.max(0,Math.min(100,availabilityPrecise));
  const availabilityPct=Math.round(availabilityPrecise??0);
  const metrics=[
    {icon:<Cpu size={14}/>,label:'CPU 1m',value:fmtPct(health['CPU 1m %'],'N/A'),mono:false},
    {icon:<MemoryStick size={14}/>,label:'Memory used',value:fmtPct(health['Memory %'],'N/A'),mono:false},
    {icon:<Clock size={14}/>,label:'Uptime',value:fmtUptime(health['Uptime']),mono:true},
    {icon:<Terminal size={14}/>,label:'OS version',value:clean(health['OS']),mono:true},
  ];
  const statusClass=(v:any)=>{const s=String(v).toLowerCase();if(/healthy|normal|^up$|forwarding/.test(s))return'ok';if(/warn|degrad|listening|learning/.test(s))return'warn';if(/crit|down|fault|error|block/.test(s))return'crit';return'neutral'};
  const alertClass=(v:any)=>{const s=String(v).toLowerCase();if(/emergency|alert|critical|error|^p1$/.test(s))return'crit';if(/warning|^p2$/.test(s))return'warn';return'neutral'};
  const cell=(col:string,value:any)=>{
    if(col==='Status'&&value!=null&&value!=='')return <span className={'dv-status dv-'+statusClass(value)}><span className="dv-dot"/>{String(value)}</span>;
    if((col==='Severity'||col==='Priority')&&value!=null&&value!=='')return <span className={'dv-alert-level dv-'+alertClass(value)}>{String(value)}</span>;
    const shown=value??'Not available from LogicMonitor';
    return <span className={missingText(shown)?'dv-missing':undefined}>{String(shown)}</span>;
  };
  return <div className="detail-view">
    <button className="dv-back" onClick={back}><ArrowLeft size={15}/> Device Fleet</button>
    <section className="dv-hero">
      <div className="dv-hero-accent"/>
      <div className="dv-hero-main">
        <span className="dv-eyebrow">{device.site} · {device.role}</span>
        <h1 className="dv-title">{device.hostname}</h1>
        <div className="dv-meta">
          <span><Router size={14}/> {device.management_ip||'No management IP'}</span>
          <span><Cpu size={14}/> {device.model||'Model pending'}</span>
        </div>
      </div>
      <div className="dv-hero-state">
        <span className="dv-chip"><span className="dv-dot"/> Monitoring state</span>
        <div className={'dv-state-value dv-'+(device.match_status==='Matched'?'ok':'warn')}>{device.match_status}</div>
        {administrator&&<button className="ds-btn accent" style={{marginTop:12}} onClick={openCollect}><Terminal size={14}/> Fill gaps from device</button>}
      </div>
    </section>
    {data.ssh&&data.ssh.identity_check&&!data.ssh.identity_check.match&&<div className="dv-ssh-banner" style={{background:'var(--dv-crit-bg,#4a1414)',borderColor:'var(--dv-crit,#e14c4c)'}}><AlertTriangle size={14} color="var(--dv-crit,#e14c4c)"/><span><b>Device identity mismatch:</b> this record expects <b>{data.ssh.identity_check.expected}</b>, but the device answering at this Management IP identifies itself as <b>{data.ssh.identity_check.reported}</b>. Every table below reflects the WRONG device. This usually means LogicMonitor's inventory has a stale/incorrect Management IP for this record — correct it there, then re-run Fill gaps.</span></div>}
    {data.ssh&&<div className="dv-ssh-banner"><Terminal size={14}/><span>Device data collected manually {data.ssh.collected_at?new Date(data.ssh.collected_at).toLocaleString():''}{data.ssh.collected_by?` by ${data.ssh.collected_by}`:''}. Shown until the next manual collection.</span>{data.ssh.has_config&&administrator&&<button className="ds-btn ghost sm" onClick={viewCfg}>View running-config</button>}</div>}
    <div className="dv-ribbon">
      {metrics.map(m=><div className="dv-metric" key={m.label}><span className="dv-metric-label">{m.icon} {m.label}</span><span className={'dv-metric-value'+(m.mono?' dv-metric-sm':'')}>{m.value}</span></div>)}
    </div>
    <nav className="dv-tabs">{data.tables.map((x:any)=><button key={x.name} className={tab===x.name?'active':''} onClick={()=>setTab(x.name)}>{x.name}</button>)}</nav>
    <section className="dv-panel">
      <div className="dv-panel-head">
        <div><h2>{table.name}</h2><p>Source: {data.source}</p></div>
        <div className="dv-panel-head-right">
          {availability!=null&&<div className="dv-ring" style={{'--pct':availabilityPct} as any} title="24h availability"><span>{fmtPct(availability)}</span></div>}
          <span className={'dv-evidence '+(table.rows.length?'available':'missing')}>{table.evidence_status}</span>
        </div>
      </div>
      {table.name==='CDP-LLDP Neighbors'&&table.rows.length
        ?<NeighborTopology device={device} rows={table.rows}/>
        :table.rows.length
          ?<div className="dv-table-scroll"><table className="dv-table"><thead><tr>{displayColumns.map((c:string)=><th key={c}>{c}</th>)}</tr></thead><tbody>{table.rows.map((row:any,i:number)=><tr key={i} className={table.name==='Alerts'?'dv-alert-row-'+alertClass(row.Severity):undefined}>{displayColumns.map((c:string)=><td key={c}>{cell(c,row[c])}</td>)}</tr>)}</tbody></table></div>
          :<div className="dv-empty"><AlertTriangle size={26}/><h3>{table.evidence_status}</h3><p>This table is ready and will populate when the corresponding LogicMonitor DataSource is discovered and authorized{administrator?', or when an administrator fills it via a manual device collection':''}. Missing evidence is not displayed as zero.</p><div className="dv-column-list">{displayColumns.map((c:string)=><span key={c}>{c}</span>)}</div></div>}
    </section>
    {sshOpen&&<div className="dv-modal-back" onClick={()=>!sshBusy&&setSshOpen(false)}><div className="dv-modal dv-modal-wide" onClick={e=>e.stopPropagation()}>
      <h2>Fill gaps from the device</h2>
      <p className="dv-modal-sub">Runs read-only Cisco <code>show</code> commands on <b>{plan?.management_ip||device.management_ip||'the device'}</b> as <b>pa-phessamfar</b> — no configuration change is made. The password is used for one session and never stored.{plan?.gaps?.length?<> Current gaps: <b>{plan.gaps.join(', ')}</b>.</>:null}</p>
      <label className="dv-modal-label">Connect now — you'll approve the sign-in on Microsoft Authenticator</label>
      <div style={{display:'flex',gap:8}}>
        <input type="password" className="dv-modal-input" style={{flex:1}} value={pw} disabled={sshBusy} autoFocus onChange={e=>setPw(e.target.value)} onKeyDown={e=>e.key==='Enter'&&pw&&!sshBusy&&runConnect()} placeholder="SSH password"/>
        <button className="ds-btn accent" style={{whiteSpace:'nowrap'}} onClick={runConnect} disabled={sshBusy||!pw||!(plan?.management_ip||device.management_ip)}>{sshBusy?'Waiting for approval…':'Connect & collect'}</button>
      </div>
      {sshMsg&&<p className={sshBusy?'dv-modal-sub':'dv-ssh-err'} style={{marginTop:12,display:'flex',alignItems:'center',gap:8}}>{sshBusy&&<RefreshCw size={14} className="dv-spin"/>}{sshMsg}</p>}
      <details style={{marginTop:16}}><summary style={{cursor:'pointer',fontSize:13,color:'var(--dv-on-variant)'}}>Or paste output manually (if the app can't reach the device)</summary>
        <label className="dv-modal-label" style={{marginTop:12}}>1 · Commands to run (copy)</label>
        <div style={{position:'relative'}}><pre className="dv-config" style={{maxHeight:120}}>{plan?.script||'…'}</pre><button className="ds-btn sm" style={{position:'absolute',top:8,right:8}} onClick={()=>{navigator.clipboard&&navigator.clipboard.writeText(plan?.script||'')}}>Copy</button></div>
        <label className="dv-modal-label" style={{marginTop:12}}>2 · Paste the full output</label>
        <textarea className="dv-modal-input" style={{minHeight:110,resize:'vertical',fontFamily:'var(--dv-mono)',fontSize:12}} value={transcript} onChange={e=>setTranscript(e.target.value)} placeholder="Paste everything from your SSH session here…"/>
        <div style={{display:'flex',justifyContent:'flex-end',marginTop:8}}><button className="ds-btn" onClick={runCollect} disabled={sshBusy||!transcript.trim()}>{sshBusy?'Parsing…':'Parse & fill gaps'}</button></div>
      </details>
      <div className="dv-modal-actions"><button className="ds-btn" onClick={()=>setSshOpen(false)} disabled={sshBusy}>Close</button></div>
    </div></div>}
    {cfg!=null&&<div className="dv-modal-back" onClick={()=>setCfg(null)}><div className="dv-modal dv-modal-wide" onClick={e=>e.stopPropagation()}>
      <h2>Running configuration <span className="dv-modal-sub" style={{fontWeight:400}}>(read-only, via SSH)</span></h2>
      <pre className="dv-config">{cfg}</pre>
      <div className="dv-modal-actions"><button className="ds-btn" onClick={()=>setCfg(null)}>Close</button></div>
    </div></div>}
  </div>;
}
function APs({devices}:{devices:Device[]}){return <section className="panel"><h2>Cisco wireless access points</h2><p className="muted">9120 and 9130 operational metrics are discovered from AP or controller DataSources.</p><div className="cards compact">{devices.map(d=><article><span>{d.site}</span><strong className="device">{d.hostname}</strong><small>{d.model||'Model pending'} · {d.match_status}</small><div className="metrics"><i>Clients <b>N/A</b></i><i>Radio use <b>N/A</b></i><i>Channel <b>N/A</b></i></div></article>)}</div>{!devices.length&&<div className="empty"><Radio/><h3>No access points</h3><p>Add a 9120 or 9130 in Settings or import a CSV.</p></div>}</section>}
const settingCategories=['General','LogicMonitor Connection','Device Inventory','Site Codes','Device Naming','Metric Mappings','Health Thresholds','SLA Targets','Collection Schedule','Reports and Retention','Users and Roles','Appearance','Audit Log'];
function SettingsPage({reload}:{reload:()=>void}){const[section,setSection]=useState('Device Inventory');return <div className="settings-layout"><nav className="settings-nav" aria-label="Settings categories">{settingCategories.map(name=><button className={section===name?'active':''} onClick={()=>setSection(name)}>{name}</button>)}</nav><div className="settings-content">{section==='Device Inventory'?<InventorySettings reload={reload}/>:section==='Site Codes'?<SiteCodeSettings/>:section==='Device Naming'?<NamingSettings/>:section==='Appearance'?<AppearanceSettings/>:<section className="panel settings-placeholder"><h2>{section}</h2><p className="muted">This category uses the shared application configuration. No editable controls are required for the current inventory implementation.</p></section>}</div></div>}
function InventorySettings({reload}:{reload:()=>void}){const[tab,setTab]=useState<'inventory'|'add'|'bulk'>('inventory');const[sites,setSites]=useState<any[]>([]);const[zones,setZones]=useState<any[]>([]);const[types,setTypes]=useState<any[]>([]);const[data,setData]=useState<any>({items:[],total:0,page:1,pages:1});const[q,setQ]=useState('');const[pageSize,setPageSize]=useState(10);const[message,setMessage]=useState('');const[f,setF]=useState<any>({site_code:'CAD03',zone:'Z01',device_type:'DSW',device_number:'01',criticality:'Medium',critical_interfaces:[],ignored_interfaces:[],tags:[],notes:'',enabled:true});const[lm,setLm]=useState<any>(null);const[job,setJob]=useState<any>(null);const loadRefs=()=>Promise.all([api('/settings/sites'),api('/settings/zones'),api('/settings/device-types')]).then(([a,b,c])=>{setSites(a);setZones(b);setTypes(c)});const loadInventory=(page=1,size=pageSize)=>api(`/inventory/devices?q=${encodeURIComponent(q)}&page=${page}&page_size=${size}`).then(setData);useEffect(()=>{loadRefs();loadInventory()},[]);const site=sites.find(x=>x.site_code===f.site_code);const dtype=types.find(x=>x.type_code===f.device_type);const generated=[f.site_code,f.zone,f.device_type,String(f.device_number).padStart(2,'0')].join('-');const save=async(again=false)=>{try{await api('/inventory/devices',{method:'POST',body:JSON.stringify({...f,device_number:String(f.device_number).padStart(2,'0'),management_ip:lm?.management_ip||null,model:lm?.model||null,manufacturer:lm?.manufacturer||null,logicmonitor_device_id:lm?.logicmonitor_device_id||null,logicmonitor_display_name:lm?.display_name||null,logicmonitor_groups:Array.isArray(lm?.groups)?lm.groups:[],logicmonitor_match_status:lm?'Matched':'Pending LogicMonitor Match',logicmonitor_match_confidence:lm?'Exact':null})});setMessage(`${generated} saved.`);loadInventory();reload();if(!again)setTab('inventory');else setF({...f,device_number:String(Number(f.device_number)+1).padStart(2,'0')})}catch(x:any){setMessage(x.message)}};return <><div className="settings-toolbar"><div><h2>Device Inventory</h2><p>Controlled naming and read-only LogicMonitor synchronization</p></div><div className="segmented"><button className={tab==='inventory'?'active':''} onClick={()=>setTab('inventory')}>Inventory</button><button className={tab==='add'?'active':''} onClick={()=>setTab('add')}>Add One Device</button><button className={tab==='bulk'?'active':''} onClick={()=>setTab('bulk')}>Bulk Import</button></div></div>{message&&<div className="form-message">{message}</div>}{tab==='inventory'&&<section className="panel inventory-panel"><div className="inventory-controls"><label>Search inventory<input value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>e.key==='Enter'&&loadInventory()}/></label><button onClick={()=>loadInventory()}>Search</button><select aria-label="Filter site" onChange={e=>api(`/inventory/devices?site=${e.target.value}`).then(setData)}><option value="">All sites</option>{sites.map(x=><option>{x.site_code}</option>)}</select><select aria-label="Filter status" onChange={e=>api(`/inventory/devices?match_status=${encodeURIComponent(e.target.value)}`).then(setData)}><option value="">All match statuses</option><option>Matched</option><option>Pending LogicMonitor Match</option></select><a className="button secondary" href="/api/v1/inventory/import/template" onClick={e=>{e.preventDefault();fetch('/api/v1/inventory/import/template',{headers:{Authorization:`Bearer ${getToken()}`}}).then(r=>r.blob()).then(b=>{const u=URL.createObjectURL(b);const a=document.createElement('a');a.href=u;a.download='Device_Inventory_Import_Template.xlsx';a.click();URL.revokeObjectURL(u)})}}>Export Template</a></div><div className="table-scroll"><table><thead><tr>{['Complete Device Name','Site Code','City','Zone','Device Type','Number of Switches','Role','Model','Management IP','LogicMonitor Device ID','LogicMonitor Match Status','Criticality','Enabled','Last Synchronized','Actions'].map(x=><th>{x}</th>)}</tr></thead><tbody>{data.items.map((d:any)=><tr><td><b>{d.generated_name}</b></td><td>{d.site_code}</td><td>{d.city}</td><td>{d.zone}</td><td>{d.device_type}</td><td title={d.physical_switch_count_source}>{d.physical_switch_count??'Not monitored'}</td><td>{d.role}</td><td>{d.model||'Pending'}</td><td>{d.management_ip||'Pending'}</td><td>{d.logicmonitor_device_id||'—'}</td><td><span className={'status-badge '+(d.logicmonitor_match_status==='Matched'?'ok':'warn')}>{d.logicmonitor_match_status}</span></td><td>{d.criticality}</td><td>{d.enabled?'Enabled':'Disabled'}</td><td>{d.last_synchronized?new Date(d.last_synchronized).toLocaleString():'Never'}</td><td className="actions"><button onClick={async()=>{await api(`/inventory/${d.id}/refresh`,{method:'POST'});loadInventory()}}>Refresh</button><button onClick={async()=>{if(confirm(`Delete ${d.generated_name} locally?`)){await api(`/inventory/devices/${d.id}`,{method:'DELETE'});loadInventory()}}}>Delete</button></td></tr>)}</tbody></table></div><div className="pagination" style={{justifyContent:'space-between'}}><label className="ds-pager-size">Show <select value={pageSize} onChange={e=>{const s=Number(e.target.value);setPageSize(s);loadInventory(1,s)}}>{[10,20,50,100].map(s=><option key={s} value={s}>{s}</option>)}</select> per page</label><div style={{display:'flex',alignItems:'center',gap:12}}><span>Page {data.page} of {data.pages} · {data.total} devices</span><button disabled={data.page<=1} onClick={()=>loadInventory(data.page-1)}>Previous</button><button disabled={data.page>=data.pages} onClick={()=>loadInventory(data.page+1)}>Next</button></div></div></section>}{tab==='add'&&<section className="panel device-form"><h2>A. Naming</h2><div className="form-grid"><label>Site Code<select value={f.site_code} onChange={e=>{setF({...f,site_code:e.target.value});setLm(null)}}>{sites.filter(x=>x.enabled).map(x=><option value={x.site_code}>{x.site_code} — {x.city}</option>)}</select></label><label>City<input value={site?.city||''} readOnly/><small>Source: Derived</small></label><label>Zone<select value={f.zone} onChange={e=>{setF({...f,zone:e.target.value});setLm(null)}}>{zones.filter(x=>x.enabled).map(x=><option value={x.zone_code}>{x.zone_code} — {x.description}</option>)}</select></label><label>Device Type<select value={f.device_type} onChange={e=>{setF({...f,device_type:e.target.value});setLm(null)}}>{types.filter(x=>x.enabled).map(x=><option value={x.type_code}>{x.type_code} — {x.description}</option>)}</select></label><label>Device Number<select value={f.device_number} onChange={e=>{setF({...f,device_number:e.target.value});setLm(null)}}>{Array.from({length:99},(_,i)=>String(i+1).padStart(2,'0')).map(x=><option>{x}</option>)}</select></label><label>Role<input value={dtype?.derived_role||''} readOnly/><small>Source: Derived</small></label></div><div className="name-preview"><span>Generated Device Name</span><strong>{generated}</strong><small>Manual hostname editing is disabled</small></div><h2>B. LogicMonitor Lookup</h2><div className="lookup-grid"><button onClick={async()=>{setMessage('Searching LogicMonitor…');try{const result=await api('/inventory/logicmonitor-lookup',{method:'POST',body:JSON.stringify(f)});setLm(result.status==='Matched'?result.candidates[0]:null);setMessage(result.status)}catch(x:any){setMessage(x.message)}}}>Search LogicMonitor</button><dl><div><dt>Status</dt><dd>{lm?'Matched':'Not searched'}</dd></div><div><dt>Display Name</dt><dd>{lm?.display_name||'—'}</dd></div><div><dt>Device ID</dt><dd>{lm?.logicmonitor_device_id||'—'}</dd></div><div><dt>Management IP</dt><dd>{lm?.management_ip||'—'}</dd></div><div><dt>Model</dt><dd>{lm?.model||'—'}</dd></div></dl></div><h2>C. Validation Summary</h2><ul className="validation-list"><li>✓ Naming sections selected</li><li>✓ City and role derived from controlled records</li><li>✓ Device number uses two digits</li><li>{lm?'✓ LogicMonitor exact match':'△ LogicMonitor match pending'}</li></ul><div className="form-actions"><button type="button" className="secondary" onClick={()=>setTab('inventory')}>Cancel</button><button type="button" className="secondary" onClick={async()=>{try{const r=await api('/inventory/preview-name',{method:'POST',body:JSON.stringify(f)});setMessage(r.unique?'Validation passed':'Generated name already exists')}catch(x:any){setMessage(x.message)}}}>Validate</button><button type="button" onClick={()=>save(false)}>Save Device</button><button type="button" onClick={()=>save(true)}>Save and Add Another</button></div></section>}{tab==='bulk'&&<section className="panel bulk-panel"><div className="workflow">{['Download Template','Fill Device Records','Upload File','Validate','Preview','Resolve Errors','Import','View Results'].map((x,i)=><span><b>{i+1}</b>{x}</span>)}</div><a className="button secondary" href="/api/v1/inventory/import/template" onClick={e=>{e.preventDefault();fetch('/api/v1/inventory/import/template',{headers:{Authorization:`Bearer ${getToken()}`}}).then(r=>r.blob()).then(b=>{const u=URL.createObjectURL(b);const a=document.createElement('a');a.href=u;a.download='Device_Inventory_Import_Template.xlsx';a.click();URL.revokeObjectURL(u)})}}>Download Excel Template</a><label className="upload-area">Upload .xlsx or .csv<input type="file" accept=".xlsx,.csv" onChange={async e=>{const file=e.target.files?.[0];if(!file)return;const fd=new FormData();fd.append('file',file);try{setJob(await api('/inventory/import/validate',{method:'POST',body:fd}));setMessage('Validation complete')}catch(x:any){setMessage(x.message)}}}/></label>{job&&<><div className="import-summary"><span>Total <b>{job.summary.total}</b></span><span>Ready <b>{job.summary.ready}</b></span><span>Errors <b>{job.summary.errors}</b></span></div><div className="table-scroll"><table><thead><tr>{['row_number','generated_name','city','role','validation_status','validation_message'].map(x=><th>{x.replaceAll('_',' ')}</th>)}</tr></thead><tbody>{job.rows.map((r:any)=><tr>{['row_number','generated_name','city','role','validation_status','validation_message'].map(k=><td>{r[k]}</td>)}</tr>)}</tbody></table></div><div className="form-actions"><button className="secondary" onClick={()=>setJob(null)}>Cancel</button><button onClick={async()=>{const result=await api(`/inventory/import/commit?job_id=${job.job_id}&mode=valid_rows_only`,{method:'POST'});setMessage(`Imported ${result.imported}; skipped ${result.skipped}`);loadInventory();reload()}}>Import All Valid Rows</button></div></>}</section>}</>}
function SiteCodeSettings(){const[rows,setRows]=useState<any[]>([]);const[q,setQ]=useState('');const[msg,setMsg]=useState('');const[f,setF]=useState<any>({site_code:'',city:'',province_region:'',country:'Canada',description:'',enabled:true});const load=()=>api(`/settings/sites?q=${encodeURIComponent(q)}`).then(setRows);useEffect(()=>{load()},[]);return <><div className="settings-toolbar"><div><h2>Site Codes</h2><p>Local naming records; LogicMonitor is never modified</p></div><button onClick={()=>{const csv=['SiteCode,City,ProvinceRegion,Country,Enabled',...rows.map(x=>[x.site_code,x.city,x.province_region,x.country,x.enabled].join(','))].join('\n');const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([csv]));a.download='Site_Codes.csv';a.click()}}>Export Site Codes</button></div><section className="panel"><form className="form-grid" onSubmit={async e=>{e.preventDefault();try{await api('/settings/sites',{method:'POST',body:JSON.stringify(f)});setMsg('Site code added');setF({...f,site_code:'',city:'',description:''});load()}catch(x:any){setMsg(x.message)}}}><label>Site Code<input value={f.site_code} onChange={e=>setF({...f,site_code:e.target.value.toUpperCase()})} required/></label><label>City<input value={f.city} onChange={e=>setF({...f,city:e.target.value})} required/></label><label>Province/Region<input value={f.province_region} onChange={e=>setF({...f,province_region:e.target.value})}/></label><label>Country<input value={f.country} onChange={e=>setF({...f,country:e.target.value})} required/></label><label className="span-2">Description<input value={f.description} onChange={e=>setF({...f,description:e.target.value})}/></label><label className="check"><input type="checkbox" checked={f.enabled} onChange={e=>setF({...f,enabled:e.target.checked})}/> Enabled</label><button>Add Site Code</button></form>{msg&&<p className="form-message">{msg}</p>}</section><section className="panel"><div className="inventory-controls"><label>Search site codes<input value={q} onChange={e=>setQ(e.target.value)}/></label><button onClick={load}>Search</button></div><div className="table-scroll"><table><thead><tr>{['Site Code','City','Province/Region','Country','Number of Switches','Enabled','Last Modified','Modified By','Actions'].map(x=><th>{x}</th>)}</tr></thead><tbody>{rows.map(x=><tr><td><b>{x.site_code}</b></td><td>{x.city}</td><td>{x.province_region}</td><td>{x.country}</td><td>{x.number_of_devices}{x.switch_count_pending?` + ${x.switch_count_pending} pending`:''}</td><td>{x.enabled?'Enabled':'Disabled'}</td><td>{new Date(x.last_modified).toLocaleDateString()}</td><td>{x.modified_by}</td><td className="actions"><button onClick={async()=>{await api(`/settings/sites/${x.id}`,{method:'PUT',body:JSON.stringify({...x,enabled:!x.enabled})});load()}}>{x.enabled?'Disable':'Enable'}</button><button onClick={async()=>{if(confirm(`Delete ${x.site_code}?`)){try{await api(`/settings/sites/${x.id}`,{method:'DELETE'});load()}catch(e:any){setMsg(e.message)}}}}>Delete</button></td></tr>)}</tbody></table></div></section></>}
function NamingSettings(){
  const[zones,setZones]=useState<any[]>([]);const[types,setTypes]=useState<any[]>([]);const[msg,setMsg]=useState('');
  const[z,setZ]=useState<any>({zone_code:'',description:''});const[t,setT]=useState<any>({type_code:'',description:'',derived_role:''});
  const loadZones=()=>api('/settings/zones').then(setZones);const loadTypes=()=>api('/settings/device-types').then(setTypes);
  useEffect(()=>{loadZones();loadTypes()},[]);
  const addZone=async(e:any)=>{e.preventDefault();try{await api('/settings/zones',{method:'POST',body:JSON.stringify({zone_code:z.zone_code.toUpperCase(),description:z.description,display_order:zones.length+1,enabled:true})});setZ({zone_code:'',description:''});setMsg(`Zone ${z.zone_code.toUpperCase()} added`);loadZones()}catch(x:any){setMsg(x.message)}};
  const delZone=async(x:any)=>{if(!confirm(`Delete zone ${x.zone_code}?`))return;try{await api(`/settings/zones/${x.id}`,{method:'DELETE'});setMsg(`Zone ${x.zone_code} removed`);loadZones()}catch(e:any){setMsg(e.message)}};
  const saveZone=async(x:any,enabled:boolean)=>{try{await api(`/settings/zones/${x.id}`,{method:'PUT',body:JSON.stringify({zone_code:x.zone_code,description:x.description,display_order:x.display_order,enabled})});loadZones()}catch(e:any){setMsg(e.message)}};
  const addType=async(e:any)=>{e.preventDefault();try{await api('/settings/device-types',{method:'POST',body:JSON.stringify({type_code:t.type_code.toUpperCase(),description:t.description,derived_role:t.derived_role,display_order:types.length+1,enabled:true})});setT({type_code:'',description:'',derived_role:''});setMsg(`Device type ${t.type_code.toUpperCase()} added`);loadTypes()}catch(x:any){setMsg(x.message)}};
  const delType=async(x:any)=>{if(!confirm(`Delete device type ${x.type_code}?`))return;try{await api(`/settings/device-types/${x.id}`,{method:'DELETE'});setMsg(`Device type ${x.type_code} removed`);loadTypes()}catch(e:any){setMsg(e.message)}};
  const saveType=async(x:any,enabled:boolean)=>{try{await api(`/settings/device-types/${x.id}`,{method:'PUT',body:JSON.stringify({type_code:x.type_code,description:x.description,derived_role:x.derived_role,display_order:x.display_order,enabled})});loadTypes()}catch(e:any){setMsg(e.message)}};
  return <><div className="settings-toolbar"><div><h2>Device Naming</h2><p>[SITE CODE]-[ZONE]-[DEVICE TYPE]-[DEVICE NUMBER]</p></div></div>
    {msg&&<div className="form-message">{msg}</div>}
    <section className="panel"><h2>Zones</h2>
      <form className="form-grid" onSubmit={addZone}><label>Zone Code<input value={z.zone_code} onChange={e=>setZ({...z,zone_code:e.target.value.toUpperCase()})} placeholder="Z10" required/></label><label>Description<input value={z.description} onChange={e=>setZ({...z,description:e.target.value})} placeholder="Zone 10" required/></label><button>Add Zone</button></form>
      <div className="table-scroll"><table><thead><tr><th>Code</th><th>Description</th><th>Enabled</th><th>Actions</th></tr></thead><tbody>{zones.map(x=><tr><td><b>{x.zone_code}</b></td><td><input value={x.description} onChange={e=>setZones(zones.map(y=>y.id===x.id?{...y,description:e.target.value}:y))} onBlur={()=>saveZone(x,x.enabled)}/></td><td>{x.enabled?'Enabled':'Disabled'}</td><td className="actions"><button onClick={()=>saveZone(x,!x.enabled)}>{x.enabled?'Disable':'Enable'}</button><button onClick={()=>delZone(x)}>Delete</button></td></tr>)}</tbody></table></div>
    </section>
    <section className="panel"><h2>Device Type and Role Mappings</h2>
      <form className="form-grid" onSubmit={addType}><label>Type Code<input value={t.type_code} onChange={e=>setT({...t,type_code:e.target.value.toUpperCase()})} placeholder="CSW" required/></label><label>Description<input value={t.description} onChange={e=>setT({...t,description:e.target.value})} placeholder="Core Switch" required/></label><label>Automatically Assigned Role<input value={t.derived_role} onChange={e=>setT({...t,derived_role:e.target.value})} placeholder="Core" required/></label><button>Add Device Type</button></form>
      <div className="table-scroll"><table><thead><tr><th>Code</th><th>Description</th><th>Automatically Assigned Role</th><th>Enabled</th><th>Actions</th></tr></thead><tbody>{types.map(x=><tr><td><b>{x.type_code}</b></td><td>{x.description}</td><td>{x.derived_role}</td><td>{x.enabled?'Enabled':'Disabled'}</td><td className="actions"><button onClick={()=>saveType(x,!x.enabled)}>{x.enabled?'Disable':'Enable'}</button><button onClick={()=>delType(x)}>Delete</button></td></tr>)}</tbody></table></div>
    </section>
  </>;
}
function AppearanceSettings(){const current=localStorage.getItem('themePreference')||'system';const setMode=(mode:string)=>{localStorage.setItem('themePreference',mode);const dark=mode==='dark'||(mode==='system'&&matchMedia('(prefers-color-scheme:dark)').matches);localStorage.setItem('theme',dark?'dark':'light');document.documentElement.dataset.theme=dark?'dark':'light';location.reload()};return <section className="panel"><h2>Appearance</h2><p className="muted">Choose a theme or follow the operating-system preference.</p><div className="theme-options">{['light','dark','system'].map(x=><button className={current===x?'active':''} onClick={()=>setMode(x)}>{x[0].toUpperCase()+x.slice(1)}</button>)}</div></section>}
function SlaPage({role}:{role:string}){
  const[data,setData]=useState<any>(null);const[msg,setMsg]=useState('');const[busy,setBusy]=useState(false);const[tr,setTr]=useState<any>(null);
  const devPg=usePager<any>(data?.devices||[]);
  const load=()=>api('/sla/summary').then(setData).catch(x=>setMsg(x.message));
  useEffect(()=>{load();api('/trends').then(setTr).catch(()=>{})},[]);
  if(!data)return <section className="panel">Loading SLA…</section>;
  const r=data.resilience||{};
  const fmt=(w:any)=>w&&w.availability!=null?fmtPct(w.availability):(w?.status||'—');
  const backfill=async()=>{setBusy(true);setMsg('Starting backfill…');try{await api('/sla/backfill',{method:'POST'});const poll=async()=>{try{const st=await api('/sla/backfill/status');if(st.running){setMsg('Backfilling availability history from LogicMonitor… this runs in the background and can take a few minutes.');setTimeout(poll,8000)}else{if(st.error){setMsg('Backfill failed: '+st.error+'. Check server logs.')}else if(st.result){setMsg(`Backfill complete for ${st.result.devices} device(s) (${st.result.start} → ${st.result.end}).`);load()}else{setMsg('Backfill finished.');load()}setBusy(false)}}catch(x:any){setMsg('Lost status connection: '+x.message);setBusy(false)}};setTimeout(poll,2000)}catch(x:any){setMsg(x.message);setBusy(false)}};
  return <>
    <section className="cards">
      <article><span>Fleet YTD availability<Help text="Fleet-wide year-to-date availability = up eligible minutes / observed eligible minutes across every mapped switch, coverage-gated at 90%. 'Insufficient' means not enough evidence yet — it is never shown as 0%."/></span><strong>{fmt(data.fleet_ytd)}</strong><small>Target {data.target}% · coverage {fmtPct(data.fleet_ytd.coverage)}</small></article>
      <article><span>Fleet WTD availability<Help text="Fleet availability week-to-date (Monday to now, in the configured timezone). Same formula as YTD but scoped to the current week."/></span><strong>{fmt(data.fleet_wtd)}</strong><small>Week to date · {data.timezone}</small></article>
      <article><span>Below target<Help text="Number of measured devices whose YTD availability is under the SLA target. 'Measured' excludes devices that lack sufficient coverage to judge."/></span><strong className={data.below_target?'danger':''}>{data.below_target}</strong><small>of {data.measured} measured of {data.monitored}</small></article>
      <article><span>Estimated tier<Help text="Fleet network-resilience tier based on observed component redundancy (stack or dual power) and limited by the weakest critical node. Tier III/IV require independently verified failure domains and are never inferred; this is not a facility certification."/></span><strong>{r.fleet_tier||'Insufficient'}</strong><small>Network resilience</small></article>
    </section>
    <section className="panel">
      <div className="panelhead"><div><h2>Uptime tier comparison<Help text="Where fleet availability sits against the Uptime Institute tier availability budgets (Tier I 99.671% → Tier IV 99.995%). Tiers certify facility power/cooling; this compares our measured network availability to those thresholds. The highlighted row is the estimated band."/></h2><p className="muted">Measured availability vs Uptime Institute budgets. Tiers certify facility power/cooling; the estimate below is a network-resilience analogy, not a certification.</p></div></div>
      <div className="table-scroll"><table><thead><tr><th>Tier</th><th>Availability</th><th>Max annual downtime</th><th>Characteristics</th><th></th></tr></thead><tbody>{(r.tiers||[]).map((t:any)=><tr key={t.tier} className={t.tier===r.fleet_tier?'sla-current':''}><td><b>{t.tier}</b></td><td>{t.availability}%</td><td>{t.max_annual_downtime}</td><td>{t.summary}</td><td>{t.tier===r.fleet_tier?'◀ estimated':''}</td></tr>)}</tbody></table></div>
    </section>
    <section className="panel">
      <div className="panelhead"><div><h2>Network resilience estimate<Help text="Why the fleet lands at its tier: a stack or dual healthy power supplies are direct N+1 component evidence. Neighbor names do not prove independent failure domains, so Tier III/IV are withheld until independently verified. Reassessed every 12 hours."/></h2><p className="muted">Recomputed every 12 hours{r.as_of?` · last run ${new Date(r.as_of).toLocaleString()}`:' · not run yet'}</p></div>{role==='Administrator'&&<button disabled={busy} onClick={backfill}>{busy?'Working…':'Run backfill + reassess'}</button>}</div>
      {msg&&<div className="form-message">{msg}</div>}
      <ul className="resilience-rationale">{(r.rationale||['Assessment pending.']).map((x:string,i:number)=><li key={i}>{x}</li>)}</ul>
    </section>
    <section className="panel">
      <div className="panelhead"><div><h2>Per-device SLA<Help text="WTD and YTD availability for each switch with its YTD coverage and estimated tier. Coverage-gated: cells show 'Insufficient evidence' below 90% coverage rather than a misleading number."/></h2><p className="muted">Coverage-gated; missing evidence is never shown as zero. Backfill start {data.backfill_start}.</p></div></div>
      <div className="table-scroll"><table><thead><tr>{['Device','Site','WTD','YTD','Coverage (YTD)','Est. tier'].map(h=><th key={h}>{h}</th>)}</tr></thead><tbody>{devPg.slice.map((d:any)=>{const dev=(r.devices||[]).find((x:any)=>x.device_id===d.device_id);return <tr key={d.device_id}><td><b>{d.hostname}</b></td><td>{d.site}</td><td>{fmt(d.wtd)}</td><td>{fmt(d.ytd)}</td><td>{fmtPct(d.ytd.coverage)}</td><td>{dev?.tier||'—'}</td></tr>})}{!data.devices.length&&<tr><td colSpan={6} className="muted">No mapped switches yet. Map switches in Settings, run collection, then backfill.</td></tr>}</tbody></table></div>
      {data.devices.length>0&&<Pager {...devPg} label="devices"/>}
    </section>
    {tr&&<section className="panel">
      <div className="panelhead"><div><h2>Trend intelligence &amp; incidents<Help text="Week-over-week and month-over-month availability change (IMPROVING/WORSENING), plus incidents derived from contiguous below-100% coverage-gated days and approximate MTTR/MTBF. Exact incident timings require LogicMonitor alert history; these are availability-derived."/></h2><p className="muted">Availability-derived and coverage-gated. {tr.mttr_mtbf.basis}</p></div></div>
      <section className="cards">
        <article><span>Week-over-week</span><strong>{fmtPctDelta(tr.deltas.wow.delta)}</strong><small>{tr.deltas.wow.trend}</small></article>
        <article><span>Month-over-month</span><strong>{fmtPctDelta(tr.deltas.mom.delta)}</strong><small>{tr.deltas.mom.trend}</small></article>
        <article><span>Incidents ({tr.mttr_mtbf.window_days}d)</span><strong>{tr.mttr_mtbf.incidents}</strong><small>availability-derived</small></article>
        <article><span>MTTR</span><strong>{tr.mttr_mtbf.mttr_minutes!=null?tr.mttr_mtbf.mttr_minutes+' min':'—'}</strong><small>mean time to recover</small></article>
        <article><span>MTBF</span><strong>{tr.mttr_mtbf.mtbf_hours!=null?tr.mttr_mtbf.mtbf_hours+' h':'—'}</strong><small>mean time between failures</small></article>
      </section>
      {tr.incidents.length>0&&<div className="table-scroll"><table><thead><tr>{['Device','Site','Start','End','Days','Downtime (min)'].map(x=><th key={x}>{x}</th>)}</tr></thead><tbody>{tr.incidents.slice(0,10).map((x:any,i:number)=><tr key={i}><td><b>{x.device}</b></td><td>{x.city}</td><td>{x.start}</td><td>{x.end}</td><td>{x.days}</td><td>{x.down}</td></tr>)}</tbody></table></div>}
    </section>}
  </>;
}
function TelemetryPage(){
  const[t,setT]=useState<any>(null);const[err,setErr]=useState('');
  const routePg=usePager<any>(t?.routing?.items||[]);const issuePg=usePager<any>(t?.interfaces?.issues||[]);
  useEffect(()=>{api('/telemetry').then(setT).catch(x=>setErr(x.message))},[]);
  if(err)return <div className="ov"><div className="ov-summary"><p>{err}</p></div></div>;
  if(!t)return <div className="ov"><div className="ov-summary"><p>Loading network telemetry…</p></div></div>;
  const badge=(s:string)=>{const u=String(s).toUpperCase();return /CRIT|FAULT|DOWN|DEGRAD/.test(u)?'crit':/HIGH|WARN|PARTIAL/.test(u)?'warn':/NOT MONITORED|NOT AVAILABLE|UNKNOWN/.test(u)?'unknown':'ok'};
  const r=t.routing,iface=t.interfaces,lat=t.latency,cov=t.data_coverage;
  const tiles=(items:any[])=><div className="ov-kpis" style={{gridTemplateColumns:'repeat(auto-fit,minmax(160px,1fr))'}}>{items.map(([l,v,s,warn]:any)=><div className="ov-card" key={l} style={{minHeight:0}}><h3>{l}</h3><div className="center" style={{gap:2}}><div className="ov-ring-val" style={{fontSize:v==='NOT MONITORED'?15:28,fontFamily:v==='NOT MONITORED'?'var(--ov-mono)':undefined,color:warn?'var(--ov-warn)':'var(--ov-on)'}}>{v}</div><div className="ov-sub">{s}</div></div></div>)}</div>;
  return <div className="ov">
    <div className="ov-exec"><div><div className="ov-eyebrow">MEDLINE CANADA</div><h1 className="ov-title">Network Telemetry</h1></div></div>
    <div className="ov-panel"><div className="ov-panel-head"><h2>Data Coverage<Help text="An honest inventory of what LogicMonitor actually exposes for this fleet, per metric: Monitored, Partial, Not monitored, or Not available — with the datasource and device count. This is why some sections show 'not monitored' instead of guessing."/></h2><span className="tag">What LogicMonitor exposes for this fleet</span></div>
      <div className="ov-table-wrap"><table className="ov-table"><thead><tr>{['Metric','LogicMonitor source','Coverage','Devices'].map(x=><th key={x}>{x}</th>)}</tr></thead><tbody>{cov.metrics.map((m:any,i:number)=><tr key={i}><td>{m.metric}</td><td className="mono" style={{color:'var(--ov-dim)'}}>{m.source}</td><td><span className={'ov-badge '+badge(m.status)}>{m.status.toUpperCase()}</span></td><td className="mono">{m.devices}/{cov.fleet}</td></tr>)}</tbody></table></div>
    </div>
    <div className="ov-panel"><div className="ov-panel-head"><h2><Router size={15}/> Routing Health — OSPF<Help text="OSPF neighbor adjacencies per device: how many are Full vs total, and neighbor-event churn. Only OSPF is monitorable in this tenant — BGP, EIGRP and static routes have no instances and are shown as not monitored."/></h2><span className="tag">{r.devices_with_ospf}/{r.fleet} devices · BGP {r.bgp} · EIGRP {r.eigrp} · Static {r.static}</span></div>
      {r.items.length?<><div className="ov-table-wrap"><table className="ov-table"><thead><tr>{['Device','Site','Adjacencies (Full/Total)','Neighbor Events','Status'].map(x=><th key={x}>{x}</th>)}</tr></thead><tbody>{routePg.slice.map((d:any)=><tr key={d.device_id}><td className="mono"><b>{d.hostname}</b></td><td>{d.city}</td><td className="mono">{d.full}/{d.neighbors}</td><td className="mono">{d.neighbor_events}</td><td><span className={'ov-badge '+badge(d.status)}>{d.status}</span></td></tr>)}</tbody></table></div><Pager {...routePg} label="devices"/></>:<div className="ov-actions-empty" style={{color:'var(--ov-dim)'}}>No OSPF adjacencies collected yet — populates on the 30-minute collection cycle.</div>}
    </div>
    <div className="ov-panel-head" style={{background:'transparent',border:0,padding:'0 0 8px'}}><h2 style={{textTransform:'none',fontFamily:'var(--ov-sans)',fontSize:16,fontWeight:700}}>Interface / Circuit Health<Help text="Fleet-wide interface status from SNMP. Every discovered interface lands in exactly one bucket — Up, Down, Disabled, or Unknown — so Total always equals their sum; nothing is silently dropped. 'Unknown' is common and expected: LogicMonitor discovers far more interface instances than it actively polls operational status for (typically unused/never-connected ports), so those honestly show as no-data rather than a fabricated up or down. High utilisation (≥80%), tx/rx errors, and flapping are independent flags checked on top of the state buckets. The issues table lists the worst offenders."/></h2></div>
    {tiles([['Interfaces',iface.total,'total',false],['Up',iface.up,'operational',false],['Down',iface.down,'admin-up but down',iface.down>0],['Unknown',iface.unknown,'no recent LM data',false],['Disabled',iface.disabled,'admin down',false],['High util',iface.high_util,'≥80%',iface.high_util>0],['With errors',iface.with_errors,'tx/rx',iface.with_errors>0],['Flapping',iface.flapping,'flaps>0',iface.flapping>0]])}
    <div className="ov-panel-head" style={{background:'transparent',border:0,padding:'12px 0 8px'}}><h2 style={{textTransform:'none',fontFamily:'var(--ov-sans)',fontSize:16,fontWeight:700}}>Latency &amp; Loss<Help text="Round-trip latency and packet loss derived from the LogicMonitor Ping datasource across the fleet (average and worst-case). Jitter is not monitored — the Cisco IP-SLA jitter datasource has no instances in this tenant."/></h2></div>
    {tiles([['Avg latency',lat.avg_latency_ms!=null?lat.avg_latency_ms+' ms':'—','fleet mean',false],['Max latency',lat.max_latency_ms!=null?lat.max_latency_ms+' ms':'—','worst device',false],['Avg packet loss',fmtPct(lat.avg_loss_pct),'fleet mean',lat.avg_loss_pct>0],['Jitter','NOT MONITORED','CiscoSLA not configured',true]])}
    {iface.issues.length>0&&<div className="ov-panel" style={{marginTop:20}}><div className="ov-panel-head"><h2>Interface Issues</h2><span className="tag">{iface.issues.length} shown</span></div>
      <div className="ov-table-wrap"><table className="ov-table"><thead><tr>{['Device','Interface','Issue','Severity'].map(x=><th key={x}>{x}</th>)}</tr></thead><tbody>{issuePg.slice.map((is:any,i:number)=><tr key={i}><td className="mono">{is.device}</td><td className="mono">{is.interface}</td><td>{is.issue}</td><td><span className={'ov-badge '+badge(is.severity)}>{is.severity}</span></td></tr>)}</tbody></table></div>
      <Pager {...issuePg} label="issues"/>
    </div>}
  </div>;
}
createRoot(document.getElementById('root')!).render(<App/>);
